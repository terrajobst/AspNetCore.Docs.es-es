---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: "Introducción a ASP.NET Web Pages: mostrar datos | Documentos de Microsoft"
author: tfitzmac
description: "Este tutorial muestra cómo crear una base de datos en WebMatrix y cómo mostrar datos de la base de datos en una página cuando se utiliza ASP.NET Web Pages (Razor). Se supone y..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: fdb9af0ba87c7802c63451ac7aa422e0020b5719
ms.sourcegitcommit: 9a9483aceb34591c97451997036a9120c3fe2baf
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 11/10/2017
---
<a name="introducing-aspnet-web-pages---displaying-data"></a><span data-ttu-id="705dc-104">Introducción a ASP.NET Web Pages: mostrar datos</span><span class="sxs-lookup"><span data-stu-id="705dc-104">Introducing ASP.NET Web Pages - Displaying Data</span></span>
====================
<span data-ttu-id="705dc-105">por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="705dc-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="705dc-106">Este tutorial muestra cómo crear una base de datos en WebMatrix y cómo mostrar datos de la base de datos en una página cuando se utiliza ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="705dc-106">This tutorial shows you how to create a database in WebMatrix and how to display database data in a page when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="705dc-107">Supone que ha completado la serie a través de [Introducción a la programación de páginas Web de ASP.NET](../introducing-razor-syntax-c.md).</span><span class="sxs-lookup"><span data-stu-id="705dc-107">It assumes you have completed the series through [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md).</span></span>
> 
> <span data-ttu-id="705dc-108">Lo que aprenderá:</span><span class="sxs-lookup"><span data-stu-id="705dc-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="705dc-109">Cómo utilizar las herramientas de WebMatrix para crear una base de datos y tablas de base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-109">How to use WebMatrix tools to create a database and database tables.</span></span>
> - <span data-ttu-id="705dc-110">Cómo utilizar las herramientas de WebMatrix para agregar datos a una base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-110">How to use WebMatrix tools to add data to a database.</span></span>
> - <span data-ttu-id="705dc-111">Cómo mostrar los datos de la base de datos en una página.</span><span class="sxs-lookup"><span data-stu-id="705dc-111">How to display data from the database on a page.</span></span>
> - <span data-ttu-id="705dc-112">Describe cómo ejecutar comandos SQL en ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="705dc-112">How to run SQL commands in ASP.NET Web Pages.</span></span>
> - <span data-ttu-id="705dc-113">Cómo personalizar la `WebGrid` auxiliar para cambiar la presentación de datos y agregar paginación y la ordenación.</span><span class="sxs-lookup"><span data-stu-id="705dc-113">How to customize the `WebGrid` helper to change the data display and to add paging and sorting.</span></span>
>   
> 
> <span data-ttu-id="705dc-114">Características y tecnologías que se tratan:</span><span class="sxs-lookup"><span data-stu-id="705dc-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="705dc-115">Herramientas de base de datos de WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="705dc-115">WebMatrix database tools.</span></span>
> - <span data-ttu-id="705dc-116">`WebGrid`aplicación auxiliar.</span><span class="sxs-lookup"><span data-stu-id="705dc-116">`WebGrid` helper.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="705dc-117">Lo que vamos a compilar</span><span class="sxs-lookup"><span data-stu-id="705dc-117">What You'll Build</span></span>

<span data-ttu-id="705dc-118">En el tutorial anterior, se han incorporado a ASP.NET Web Pages (*.cshtml* archivos), los conceptos básicos de la sintaxis Razor y aplicaciones auxiliares.</span><span class="sxs-lookup"><span data-stu-id="705dc-118">In the previous tutorial, you were introduced to ASP.NET Web Pages (*.cshtml* files), to the basics of Razor syntax, and to helpers.</span></span> <span data-ttu-id="705dc-119">En este tutorial, podrá empezar a crear la aplicación web real que va a utilizar para el resto de la serie.</span><span class="sxs-lookup"><span data-stu-id="705dc-119">In this tutorial, you'll begin creating the actual web application that you'll use for the rest of the series.</span></span> <span data-ttu-id="705dc-120">La aplicación es una aplicación de película simple que le permite ver, agregar, cambiar y eliminar información sobre las películas.</span><span class="sxs-lookup"><span data-stu-id="705dc-120">The app is a simple movie application that lets you view, add, change, and delete information about movies.</span></span>

<span data-ttu-id="705dc-121">Cuando haya terminado con este tutorial, podrá ver una lista de película similar a esta página:</span><span class="sxs-lookup"><span data-stu-id="705dc-121">When you're done with this tutorial, you'll be able to view a movie listing that looks like this page:</span></span>

![Pantalla de WebGrid que incluye los parámetros establecidos en los nombres de clase CSS](displaying-data/_static/image1.png)

<span data-ttu-id="705dc-123">Pero, para empezar, deberá crear una base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-123">But to begin, you have to create a database.</span></span>

## <a name="a-very-brief-introduction-to-databases"></a><span data-ttu-id="705dc-124">Una breve introducción a las bases de datos</span><span class="sxs-lookup"><span data-stu-id="705dc-124">A Very Brief Introduction to Databases</span></span>

<span data-ttu-id="705dc-125">Este tutorial proporcionará sólo la briefest Introducción a las bases de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-125">This tutorial will provide only the briefest introduction to databases.</span></span> <span data-ttu-id="705dc-126">Si tiene experiencia en base de datos, puede omitir esta sección corta.</span><span class="sxs-lookup"><span data-stu-id="705dc-126">If you have database experience, you can skip this short section.</span></span>

<span data-ttu-id="705dc-127">Una base de datos contiene una o más tablas que contienen información &mdash; por ejemplo, tablas de clientes, pedidos y los proveedores, o para estudiantes, profesores, clases y grados.</span><span class="sxs-lookup"><span data-stu-id="705dc-127">A database contains one or more tables that contain information &mdash; for example, tables for customers, orders, and vendors, or for students, teachers, classes, and grades.</span></span> <span data-ttu-id="705dc-128">Estructuralmente, una tabla de base de datos es similar a una hoja de cálculo.</span><span class="sxs-lookup"><span data-stu-id="705dc-128">Structurally, a database table is like a spreadsheet.</span></span> <span data-ttu-id="705dc-129">Imagine una libreta de direcciones típico.</span><span class="sxs-lookup"><span data-stu-id="705dc-129">Imagine a typical address book.</span></span> <span data-ttu-id="705dc-130">Para cada entrada de la libreta de direcciones (es decir, para cada persona) tienen varios elementos de información como el nombre, apellido, dirección, dirección de correo electrónico y número de teléfono.</span><span class="sxs-lookup"><span data-stu-id="705dc-130">For each entry in the address book (that is, for each person) you have several pieces of information such as first name, last name, address, email address, and phone number.</span></span>

![Tabla de base de datos de ejemplo como una cuadrícula simple](displaying-data/_static/image2.png)

<span data-ttu-id="705dc-132">(Filas se conocen a veces como *registros*, y las columnas se denominan a veces *campos*.)</span><span class="sxs-lookup"><span data-stu-id="705dc-132">(Rows are sometimes referred to as *records*, and columns are sometimes referred to as *fields*.)</span></span>

<span data-ttu-id="705dc-133">Para la mayoría de las tablas de base de datos, la tabla debe tener una columna que contiene un valor único, como un número de cliente, número de cuenta y así sucesivamente.</span><span class="sxs-lookup"><span data-stu-id="705dc-133">For most database tables, the table has to have a column that contains a unique value, like a customer number, account number, and so on.</span></span> <span data-ttu-id="705dc-134">Este valor se conoce como la tabla *clave principal*, y usarlo para identificar cada fila de la tabla.</span><span class="sxs-lookup"><span data-stu-id="705dc-134">This value is known as the table's *primary key*, and you use it to identify each row in the table.</span></span> <span data-ttu-id="705dc-135">En el ejemplo, la columna Id. es la clave principal de la libreta de direcciones que se muestra en el ejemplo anterior.</span><span class="sxs-lookup"><span data-stu-id="705dc-135">In the example, the ID column is the primary key for the address book shown in the previous example.</span></span>

<span data-ttu-id="705dc-136">Gran parte del trabajo que realice en las aplicaciones web consiste en leer información de la base de datos y mostrar en una página.</span><span class="sxs-lookup"><span data-stu-id="705dc-136">Much of the work you do in web applications consists of reading information out of the database and displaying it on a page.</span></span> <span data-ttu-id="705dc-137">A menudo, también podrá recopilar información de usuarios y agregarlo a una base de datos, o podrá modificar los registros que ya están en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-137">You'll also often gather information from users and add it to a database, or you'll modify records that are already in the database.</span></span> <span data-ttu-id="705dc-138">(Se tratarán todas estas operaciones en el transcurso de este conjunto de tutorial.)</span><span class="sxs-lookup"><span data-stu-id="705dc-138">(We'll cover all of these operations in the course of this tutorial set.)</span></span>

<span data-ttu-id="705dc-139">Trabajo de base de datos puede ser enormemente compleja y puede requerir un conocimiento especializado.</span><span class="sxs-lookup"><span data-stu-id="705dc-139">Database work can be enormously complex and can require specialized knowledge.</span></span> <span data-ttu-id="705dc-140">Sin embargo, para el conjunto de este tutorial, deberá comprender solo de los conceptos básicos, que todos se explicarán marcha.</span><span class="sxs-lookup"><span data-stu-id="705dc-140">For this tutorial set, though, you have to understand only basic concepts, which will all be explained as you go.</span></span>

## <a name="creating-a-database"></a><span data-ttu-id="705dc-141">Crear una base de datos</span><span class="sxs-lookup"><span data-stu-id="705dc-141">Creating a Database</span></span>

<span data-ttu-id="705dc-142">WebMatrix incluye herramientas que facilitan la tarea para crear una base de datos y para crear tablas en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-142">WebMatrix includes tools that make it easy to create a database and to create tables in the database.</span></span> <span data-ttu-id="705dc-143">(La estructura de una base de datos se conoce como la base de datos *esquema*.) Para el conjunto de este tutorial, creará una base de datos que tiene solo una tabla en ella &mdash; películas.</span><span class="sxs-lookup"><span data-stu-id="705dc-143">(The structure of a database is referred to as the database's *schema*.) For this tutorial set, you'll create a database that has only one table in it &mdash; Movies.</span></span>

<span data-ttu-id="705dc-144">Abra WebMatrix si aún no lo ha hecho y abra el sitio de WebPagesMovies que creó en el tutorial anterior.</span><span class="sxs-lookup"><span data-stu-id="705dc-144">Open WebMatrix if you haven't already done so, and open the WebPagesMovies site that you created in the previous tutorial.</span></span>

<span data-ttu-id="705dc-145">En el panel izquierdo, haga clic en el **base de datos** área de trabajo.</span><span class="sxs-lookup"><span data-stu-id="705dc-145">In the left pane, click the **Database** workspace.</span></span>

![Ficha de área de trabajo de la base de datos de WebMatrix](displaying-data/_static/image3.png)

<span data-ttu-id="705dc-147">Los cambios de la cinta de opciones para mostrar las tareas relacionadas con la base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-147">The ribbon changes to show database-related tasks.</span></span> <span data-ttu-id="705dc-148">En la cinta de opciones, haga clic en **nueva base de datos**.</span><span class="sxs-lookup"><span data-stu-id="705dc-148">In the ribbon, click **New Database**.</span></span>

![Botón 'Nueva base de datos' en la cinta de opciones de WebMatrix](displaying-data/_static/image4.png)

<span data-ttu-id="705dc-150">WebMatrix crea una base de datos de SQL Server CE (un *.sdf* archivo) que tiene el mismo nombre que su sitio &mdash; *WebPagesMovies.sdf*.</span><span class="sxs-lookup"><span data-stu-id="705dc-150">WebMatrix creates a SQL Server CE database (an *.sdf* file) that has the same name as your site &mdash; *WebPagesMovies.sdf*.</span></span> <span data-ttu-id="705dc-151">(No hace esto aquí, pero puede cambiar el nombre del archivo a cualquier elemento que desee, siempre y cuando tenga un *.sdf* extensión.)</span><span class="sxs-lookup"><span data-stu-id="705dc-151">(You won't do this here, but you can rename the file to anything you like, as long as it has an *.sdf* extension.)</span></span>

## <a name="creating-a-table"></a><span data-ttu-id="705dc-152">Creación de una tabla</span><span class="sxs-lookup"><span data-stu-id="705dc-152">Creating a Table</span></span>

<span data-ttu-id="705dc-153">En la cinta de opciones, haga clic en **nueva tabla**.</span><span class="sxs-lookup"><span data-stu-id="705dc-153">In the ribbon, click **New Table**.</span></span> <span data-ttu-id="705dc-154">WebMatrix abre el Diseñador de tablas en una nueva pestaña. (Si la opción de nueva tabla no está disponible, asegúrese de que la nueva base de datos está seleccionada en la vista de árbol de la izquierda.)</span><span class="sxs-lookup"><span data-stu-id="705dc-154">WebMatrix opens the table designer in a new tab. (If the New Table option isn't available, make sure that the new database is selected in the tree view on the left.)</span></span>

![Botón 'Nueva tabla' en la cinta de opciones de WebMatrix](displaying-data/_static/image5.png)

<span data-ttu-id="705dc-156">En el cuadro de texto en la parte superior (donde la marca de agua dice "Escriba el nombre de la tabla"), escriba "Películas".</span><span class="sxs-lookup"><span data-stu-id="705dc-156">In the text box at the top (where the watermark says "Enter table name"), enter "Movies".</span></span>

![Escribir un nombre de tabla en el Diseñador de la base de datos de WebMatrix](displaying-data/_static/image6.png)

<span data-ttu-id="705dc-158">El panel debajo del nombre de tabla es donde se definen las columnas individuales.</span><span class="sxs-lookup"><span data-stu-id="705dc-158">The pane underneath the table name is where you define individual columns.</span></span> <span data-ttu-id="705dc-159">Para el *películas* tabla en este tutorial, creará solo unas pocas columnas: *identificador*, *título*, *género*, y *año*.</span><span class="sxs-lookup"><span data-stu-id="705dc-159">For the *Movies* table in this tutorial, you'll create only a few columns: *ID*, *Title*, *Genre*, and *Year*.</span></span>

<span data-ttu-id="705dc-160">En el **nombre** cuadro, escriba "ID".</span><span class="sxs-lookup"><span data-stu-id="705dc-160">In the **Name** box, enter "ID".</span></span> <span data-ttu-id="705dc-161">Si se introduce un valor aquí activa todos los controles para la nueva columna.</span><span class="sxs-lookup"><span data-stu-id="705dc-161">Entering a value here activates all the controls for the new column.</span></span>

<span data-ttu-id="705dc-162">Tabulador para ir a la **tipo de datos** lista y elija **int**. Este valor especifica que la columna Id. contendrá datos enteros (número).</span><span class="sxs-lookup"><span data-stu-id="705dc-162">Tab to the **Data Type** list and choose **int**. This value specifies that the ID column will contain integer (number) data.</span></span>

> [!NOTE]
> <span data-ttu-id="705dc-163">No lo llamaremos cualquier mas informacion (mucho), pero puede utilizar movimientos de teclado estándares de Windows para desplazarse en esta cuadrícula.</span><span class="sxs-lookup"><span data-stu-id="705dc-163">We won't call it out any more here (much), but you can use standard Windows keyboard gestures to navigate in this grid.</span></span> <span data-ttu-id="705dc-164">Por ejemplo, puede cambiar mediante tabulación entre los campos, simplemente puede empezar a escribir con el fin de seleccionar un elemento de una lista y así sucesivamente.</span><span class="sxs-lookup"><span data-stu-id="705dc-164">For example, you can tab between fields, you can just start typing in order to select an item in a list, and so on.</span></span>


<span data-ttu-id="705dc-165">Tabulador, cambie la **Default Value** cuadro (es decir, déjelo en blanco).</span><span class="sxs-lookup"><span data-stu-id="705dc-165">Tab past the **Default Value** box (that is, leave it blank).</span></span> <span data-ttu-id="705dc-166">Tabulador para ir a la **es la clave principal** casilla de verificación y selecciónelo.</span><span class="sxs-lookup"><span data-stu-id="705dc-166">Tab to the **Is Primary Key** check box and select it.</span></span> <span data-ttu-id="705dc-167">Esta opción indica a la base de datos que la *identificador* columna contendrá los datos que identifica las filas individuales.</span><span class="sxs-lookup"><span data-stu-id="705dc-167">This option tells the database that the *ID* column will contain the data that identifies individual rows.</span></span> <span data-ttu-id="705dc-168">(Es decir, cada fila tendrá un valor único en la columna de identificador que puede usar para encontrar esa fila.)</span><span class="sxs-lookup"><span data-stu-id="705dc-168">(That is, each row will have a unique value in the ID column that you can use to find that row.)</span></span>

<span data-ttu-id="705dc-169">Elija la **identidad** opción.</span><span class="sxs-lookup"><span data-stu-id="705dc-169">Choose the **Is Identity** option.</span></span> <span data-ttu-id="705dc-170">Esta opción indica a la base de datos que debe generar automáticamente el siguiente número secuencial para cada nueva fila.</span><span class="sxs-lookup"><span data-stu-id="705dc-170">This option tells the database that it should automatically generate the next sequential number for each new row.</span></span> <span data-ttu-id="705dc-171">(El **identidad** opción funciona sólo si también ha seleccionado el **es la clave principal** opción.)</span><span class="sxs-lookup"><span data-stu-id="705dc-171">(The **Is Identity** option works only if you've also selected the **Is Primary Key** option.)</span></span>

<span data-ttu-id="705dc-172">Haga clic en la siguiente fila de cuadrícula, o presione la tecla Tab dos veces para finalizar la fila actual.</span><span class="sxs-lookup"><span data-stu-id="705dc-172">Click in the next grid row, or press Tab twice to finish the current row.</span></span> <span data-ttu-id="705dc-173">Cualquier movimiento guarda la fila actual y siguiente inicia.</span><span class="sxs-lookup"><span data-stu-id="705dc-173">Either gesture saves the current row and starts the next one.</span></span> <span data-ttu-id="705dc-174">Tenga en cuenta que la **Default Value** columna ahora dice **Null**.</span><span class="sxs-lookup"><span data-stu-id="705dc-174">Notice that the **Default Value** column now says **Null**.</span></span> <span data-ttu-id="705dc-175">(Null es el valor predeterminado para el valor predeterminado, por lo que decirlo.)</span><span class="sxs-lookup"><span data-stu-id="705dc-175">(Null is the default value for the default value, so to speak.)</span></span>

<span data-ttu-id="705dc-176">Cuando haya terminado de definir el nuevo **identificador** columna, el diseñador tendrá un aspecto similar de esta ilustración:</span><span class="sxs-lookup"><span data-stu-id="705dc-176">When you've finished defining the new **ID** column, the designer will look like this illustration:</span></span>

![Diseñador de la base de datos de WebMatrix después de definir la columna de Id. de la tabla de películas](displaying-data/_static/image7.png)

<span data-ttu-id="705dc-178">Para crear la columna siguiente, haga clic en el cuadro en el **nombre** columna.</span><span class="sxs-lookup"><span data-stu-id="705dc-178">To create the next column, click in the box in the **Name** column.</span></span> <span data-ttu-id="705dc-179">Escriba "Título" para la columna y, a continuación, seleccione **nvarchar** para el **tipo de datos** valor.</span><span class="sxs-lookup"><span data-stu-id="705dc-179">Enter "Title" for the column and then select **nvarchar** for the **Data Type** value.</span></span> <span data-ttu-id="705dc-180">La parte de "var" de **nvarchar** indica que la base de datos que los datos de esta columna será una cadena cuyo tamaño puede variar entre los registros.</span><span class="sxs-lookup"><span data-stu-id="705dc-180">The "var" part of **nvarchar** tells the database that the data for this column will be a string whose size might vary from record to record.</span></span> <span data-ttu-id="705dc-181">(El prefijo "n" representa "national", lo que indica que el campo puede contener datos de caracteres para cualquier alfabeto o sistema de escritura, es decir, el campo contiene datos Unicode.)</span><span class="sxs-lookup"><span data-stu-id="705dc-181">(The "n" prefix represents "national," which indicates that the field can hold character data for any alphabet or writing system — that is, the field holds Unicode data.)</span></span>

<span data-ttu-id="705dc-182">Cuando eliges **nvarchar**, aparecerá otro cuadro donde puede especificar la longitud máxima para el campo.</span><span class="sxs-lookup"><span data-stu-id="705dc-182">When you choose **nvarchar**, another box appears where you can enter the maximum length for the field.</span></span> <span data-ttu-id="705dc-183">Escriba 50, en la suposición de que ningún título de la película que va a trabajar en este tutorial será mayor que 50 caracteres.</span><span class="sxs-lookup"><span data-stu-id="705dc-183">Enter 50, on the assumption that no movie title that you'll work with in this tutorial will be longer than 50 characters.</span></span>

<span data-ttu-id="705dc-184">Omitir **Default Value** y desactive el **permitir valores NULL** opción.</span><span class="sxs-lookup"><span data-stu-id="705dc-184">Skip **Default Value** and clear the **Allow Nulls** option.</span></span> <span data-ttu-id="705dc-185">No desea permitir que las películas que deben especificarse en la base de datos que no tienen un título de la base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-185">You don't want the database to allow any movies to be entered into the database that don't have a title.</span></span>

<span data-ttu-id="705dc-186">Cuando haya terminado y desplazarse a la fila siguiente, el diseñador es similar a esta ilustración:</span><span class="sxs-lookup"><span data-stu-id="705dc-186">When you're done and move to the next row, the designer looks like this illustration:</span></span>

![Diseñador de la base de datos de WebMatrix después de definir la columna de título para la tabla de películas](displaying-data/_static/image8.png)

<span data-ttu-id="705dc-188">Repita estos pasos para crear una columna denominada "Género", excepto para la longitud, establézcalo en simplemente 30.</span><span class="sxs-lookup"><span data-stu-id="705dc-188">Repeat these steps to create a column named "Genre", except for the length, set it to just 30.</span></span>

<span data-ttu-id="705dc-189">Cree otra columna denominada "Año".</span><span class="sxs-lookup"><span data-stu-id="705dc-189">Create another column named "Year."</span></span> <span data-ttu-id="705dc-190">Para el tipo de datos, elija **nchar** (no **nvarchar**) y establecer la longitud en 4.</span><span class="sxs-lookup"><span data-stu-id="705dc-190">For the data type, choose **nchar** (not **nvarchar**) and set the length to 4.</span></span> <span data-ttu-id="705dc-191">Para el año, va a utilizar un número de 4 dígitos como "1995" o "2010", por lo que no requieren una columna de tamaño variable.</span><span class="sxs-lookup"><span data-stu-id="705dc-191">For the year, you're going to use a 4-digit number like "1995" or "2010", so you don't require a variable-sized column.</span></span>

<span data-ttu-id="705dc-192">Este es el aspecto del diseño finalizado:</span><span class="sxs-lookup"><span data-stu-id="705dc-192">Here's what the finished design looks like:</span></span>

![Diseñador de bases de datos de WebMatrix después de que todos los campos definidos para la tabla de películas](displaying-data/_static/image9.png)

<span data-ttu-id="705dc-194">Presione Ctrl + S o haga clic en el **guardar** botón en la barra de herramientas de acceso rápido.</span><span class="sxs-lookup"><span data-stu-id="705dc-194">Press Ctrl+S or click the **Save** button in the Quick Access toolbar.</span></span> <span data-ttu-id="705dc-195">Cierre el Diseñador de la base de datos, el cierre de la ficha.</span><span class="sxs-lookup"><span data-stu-id="705dc-195">Close the database designer by closing the tab.</span></span>

## <a name="adding-some-example-data"></a><span data-ttu-id="705dc-196">Agregue algunos datos de ejemplo</span><span class="sxs-lookup"><span data-stu-id="705dc-196">Adding Some Example Data</span></span>

<span data-ttu-id="705dc-197">Más adelante en esta serie de tutoriales creará una página donde puede escribir nuevas películas en un formulario.</span><span class="sxs-lookup"><span data-stu-id="705dc-197">Later in this tutorial series you'll create a page where you can enter new movies in a form.</span></span> <span data-ttu-id="705dc-198">Por ahora, sin embargo, puede agregar algunos datos de ejemplo que, a continuación, puede mostrar en una página.</span><span class="sxs-lookup"><span data-stu-id="705dc-198">For now, however, you can add some example data that you can then display on a page.</span></span>

<span data-ttu-id="705dc-199">En el **base de datos** área de trabajo de WebMatrix, observe que hay un árbol que muestra la *.sdf* archivo creado anteriormente.</span><span class="sxs-lookup"><span data-stu-id="705dc-199">In the **Database** workspace in WebMatrix, notice that there's a tree that shows you the *.sdf* file you created earlier.</span></span> <span data-ttu-id="705dc-200">Abra el nodo para el nuevo *.sdf* de archivos y, a continuación, abra el **tablas** nodo.</span><span class="sxs-lookup"><span data-stu-id="705dc-200">Open the node for your new *.sdf* file, and then open the **Tables** node.</span></span>

![Área de trabajo de base de datos de WebMatrix con árbol abierta a la tabla de películas](displaying-data/_static/image10.png)

<span data-ttu-id="705dc-202">Haga clic en el **películas** nodo y, a continuación, elija **datos**.</span><span class="sxs-lookup"><span data-stu-id="705dc-202">Right-click the **Movies** node and then choose **Data**.</span></span> <span data-ttu-id="705dc-203">WebMatrix abre una cuadrícula donde puede escribir los datos para la *películas* tabla:</span><span class="sxs-lookup"><span data-stu-id="705dc-203">WebMatrix opens a grid where you can enter data for the *Movies* table:</span></span>

![Cuadrícula de la entrada de base de datos en WebMatrix (vacío)](displaying-data/_static/image11.png)

<span data-ttu-id="705dc-205">Haga clic en el **título** columna y escriba "Cuando Harry cumplen Sally".</span><span class="sxs-lookup"><span data-stu-id="705dc-205">Click the **Title** column and enter "When Harry Met Sally".</span></span> <span data-ttu-id="705dc-206">Mover a la **género** columna (que puede utilizar la tecla Tab) y escriba "Románticos Comedia".</span><span class="sxs-lookup"><span data-stu-id="705dc-206">Move to the **Genre** column (you can use the Tab key) and enter "Romantic Comedy".</span></span> <span data-ttu-id="705dc-207">Mover a la **año** columna y escriba "1989":</span><span class="sxs-lookup"><span data-stu-id="705dc-207">Move to the **Year** column and enter "1989":</span></span>

![Cuadrícula de entrada de base de datos con un registro en WebMatrix](displaying-data/_static/image12.png)

<span data-ttu-id="705dc-209">Presione ENTRAR y WebMatrix guarda la película nuevo.</span><span class="sxs-lookup"><span data-stu-id="705dc-209">Press Enter, and WebMatrix saves the new movie.</span></span> <span data-ttu-id="705dc-210">Tenga en cuenta que la **identificador** columna ha sido rellenada.</span><span class="sxs-lookup"><span data-stu-id="705dc-210">Notice that the **ID** column has been filled in.</span></span>

![Cuadrícula de entrada de base de datos de WebMatrix con un único registro y un identificador generado automáticamente](displaying-data/_static/image13.png)

<span data-ttu-id="705dc-212">Escriba otra película (por ejemplo, "ya no existe con el viento", "Series", "1939").</span><span class="sxs-lookup"><span data-stu-id="705dc-212">Enter another movie (for example, "Gone with the Wind", "Drama", "1939").</span></span> <span data-ttu-id="705dc-213">Se rellena la columna Id. nuevo:</span><span class="sxs-lookup"><span data-stu-id="705dc-213">The ID column is filled in again:</span></span>

![Cuadrícula de entrada de base de datos de WebMatrix con dos registros y los identificadores generados automáticamente](displaying-data/_static/image14.png)

<span data-ttu-id="705dc-215">Escriba una película terceros (por ejemplo, "Ghostbusters", "Comedia").</span><span class="sxs-lookup"><span data-stu-id="705dc-215">Enter a third movie (for example, "Ghostbusters", "Comedy").</span></span> <span data-ttu-id="705dc-216">Como experimento, deje el **año** columna en blanco y, a continuación, presione ENTRAR.</span><span class="sxs-lookup"><span data-stu-id="705dc-216">As an experiment, leave the **Year** column blank and then press Enter.</span></span> <span data-ttu-id="705dc-217">Dado que anula la selección de la **permitir valores NULL** opción, la base de datos muestra un error:</span><span class="sxs-lookup"><span data-stu-id="705dc-217">Because you unselected the **Allow Nulls** option, the database shows an error:</span></span>

![Error de 'Datos no válidos' muestra si un valor de columna necesarios se deja en blanco](displaying-data/_static/image15.png)

<span data-ttu-id="705dc-219">Haga clic en **Aceptar** para volver atrás y corregir la entrada (el año correspondiente a "Ghostbusters" es 1984) y, a continuación, presione ENTRAR.</span><span class="sxs-lookup"><span data-stu-id="705dc-219">Click **OK** to go back and fix the entry (the year for "Ghostbusters" is 1984), and then press Enter.</span></span>

<span data-ttu-id="705dc-220">Rellene varias películas hasta que haya 8 o modo.</span><span class="sxs-lookup"><span data-stu-id="705dc-220">Fill in several movies until you have 8 or so.</span></span> <span data-ttu-id="705dc-221">(Escriba 8 resulta más fácil trabajar con paginación más adelante.</span><span class="sxs-lookup"><span data-stu-id="705dc-221">(Entering 8 makes it easier to work with paging later.</span></span> <span data-ttu-id="705dc-222">Pero si es un número demasiado grande, escriba algunos por ahora). No importan los datos reales.</span><span class="sxs-lookup"><span data-stu-id="705dc-222">But if that's too many, enter just a few for now.) The actual data doesn't matter.</span></span>

![Cuadrícula de entrada de base de datos de WebMatrix con cualquiera de los registros](displaying-data/_static/image16.png)

<span data-ttu-id="705dc-224">Si ha especificado todas las películas sin errores, los valores de identificador son secuenciales.</span><span class="sxs-lookup"><span data-stu-id="705dc-224">If you entered all the movies without any errors, the ID values are sequential.</span></span> <span data-ttu-id="705dc-225">Si intenta guardar un registro de película incompletos, los números de identificador no sea secuenciales.</span><span class="sxs-lookup"><span data-stu-id="705dc-225">If you tried to save an incomplete movie record, the ID numbers might not be sequential.</span></span> <span data-ttu-id="705dc-226">Si es así, que es correcto.</span><span class="sxs-lookup"><span data-stu-id="705dc-226">If so, that's okay.</span></span> <span data-ttu-id="705dc-227">Los números no tienen ningún significado inherente y lo único que es importante es que sean únicos en el *películas* tabla.</span><span class="sxs-lookup"><span data-stu-id="705dc-227">The numbers don't have any inherent meaning, and the only thing that's important is that they're unique in the *Movies* table.</span></span>

<span data-ttu-id="705dc-228">Cerrar la pestaña que contiene el Diseñador de la base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-228">Close the tab that contains the database designer.</span></span>

<span data-ttu-id="705dc-229">Ahora puede optar por mostrar estos datos en una página web.</span><span class="sxs-lookup"><span data-stu-id="705dc-229">Now you can turn to displaying this data on a web page.</span></span>

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a><span data-ttu-id="705dc-230">Mostrar datos en una página mediante la aplicación auxiliar WebGrid</span><span class="sxs-lookup"><span data-stu-id="705dc-230">Displaying Data in a Page by Using the WebGrid Helper</span></span>

<span data-ttu-id="705dc-231">Para mostrar datos en una página, va a utilizar el `WebGrid` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="705dc-231">To display data in a page, you're going to use the `WebGrid` helper.</span></span> <span data-ttu-id="705dc-232">Esta aplicación auxiliar produzca una presentación en una cuadrícula o una tabla (filas y columnas).</span><span class="sxs-lookup"><span data-stu-id="705dc-232">This helper produces a display in a grid or table (rows and columns).</span></span> <span data-ttu-id="705dc-233">Como verá, le puede refinar la cuadrícula con formato y otras características.</span><span class="sxs-lookup"><span data-stu-id="705dc-233">As you'll see, you'll be able refine the grid with formatting and other features.</span></span>

<span data-ttu-id="705dc-234">Para ejecutar la cuadrícula, tendrá que escribir unas cuantas líneas de código.</span><span class="sxs-lookup"><span data-stu-id="705dc-234">To run the grid, you'll have to write a few lines of code.</span></span> <span data-ttu-id="705dc-235">Algunas de estas líneas le servirá como un tipo de modelo para casi todo el acceso a los datos que haga en este tutorial.</span><span class="sxs-lookup"><span data-stu-id="705dc-235">These few lines will serve as a kind of pattern for almost all of the data access that you do in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="705dc-236">Realmente tiene muchas opciones para mostrar datos en una página; el `WebGrid` auxiliar es solo uno.</span><span class="sxs-lookup"><span data-stu-id="705dc-236">You actually have many options for displaying data on a page; the `WebGrid` helper is just one.</span></span> <span data-ttu-id="705dc-237">Se eligió para este tutorial porque es la manera más fácil de mostrar los datos y porque es bastante flexible.</span><span class="sxs-lookup"><span data-stu-id="705dc-237">We chose it for this tutorial because it's the easiest way to display data and because it's reasonably flexible.</span></span> <span data-ttu-id="705dc-238">En el siguiente conjunto de tutorial, verá cómo se utiliza una forma más "manual" para trabajar con datos en la página, que proporciona un control más directo sobre cómo mostrar los datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-238">In the next tutorial set, you'll see how to use a more "manual" way to work with data in the page, which gives you more direct control over how to display the data.</span></span>


<span data-ttu-id="705dc-239">En el panel izquierdo en WebMatrix, haga clic en el **archivos** área de trabajo.</span><span class="sxs-lookup"><span data-stu-id="705dc-239">In the left pane in WebMatrix, click the **Files** workspace.</span></span>

<span data-ttu-id="705dc-240">Es la nueva base de datos que creó en el *aplicación\_datos* carpeta.</span><span class="sxs-lookup"><span data-stu-id="705dc-240">The new database you created is in the *App\_Data* folder.</span></span> <span data-ttu-id="705dc-241">Si la carpeta no existe, WebMatrix creó para la nueva base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-241">If the folder didn't already exist, WebMatrix created it for your new database.</span></span> <span data-ttu-id="705dc-242">(Podría haber existido la carpeta si anteriormente ha instalado aplicaciones auxiliares).</span><span class="sxs-lookup"><span data-stu-id="705dc-242">(The folder might have existed if you'd previously installed helpers.)</span></span>

<span data-ttu-id="705dc-243">En la vista de árbol, seleccione la raíz del sitio Web.</span><span class="sxs-lookup"><span data-stu-id="705dc-243">In the tree view, select the root of the website.</span></span> <span data-ttu-id="705dc-244">Debe seleccionar la raíz del sitio Web; en caso contrario, se podría agregar el nuevo archivo a la aplicación\_carpeta de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-244">You must select the root of the website; otherwise, the new file might be added to the App\_Data folder.</span></span>

<span data-ttu-id="705dc-245">En la cinta de opciones, haga clic en **nuevo**.</span><span class="sxs-lookup"><span data-stu-id="705dc-245">In the ribbon, click **New**.</span></span> <span data-ttu-id="705dc-246">En el **elegir un tipo de archivo** cuadro, elija **CSHTML**.</span><span class="sxs-lookup"><span data-stu-id="705dc-246">In the **Choose a File Type** box, choose **CSHTML**.</span></span>

<span data-ttu-id="705dc-247">En el **nombre** cuadro, asigne el nombre de la nueva página "Movies.cshtml":</span><span class="sxs-lookup"><span data-stu-id="705dc-247">In the **Name** box, name the new page "Movies.cshtml":</span></span>

![Cuadro de diálogo 'Elegir un tipo de archivo' que muestra la página 'Películas'](displaying-data/_static/image17.png)

<span data-ttu-id="705dc-249">Haga clic en el **Aceptar** botón.</span><span class="sxs-lookup"><span data-stu-id="705dc-249">Click the **OK** button.</span></span> <span data-ttu-id="705dc-250">WebMatrix abre un nuevo archivo con algunos elementos esqueletos en ella.</span><span class="sxs-lookup"><span data-stu-id="705dc-250">WebMatrix opens a new file with some skeleton elements in it.</span></span> <span data-ttu-id="705dc-251">En primer lugar, se escribirá código para ir a obtener los datos de la base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-251">First you'll write some code to go get the data from the database.</span></span> <span data-ttu-id="705dc-252">A continuación, agregará el marcado a la página para mostrar los datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-252">Then you'll add markup to the page to actually display the data.</span></span>

### <a name="writing-the-data-query-code"></a><span data-ttu-id="705dc-253">Escribir el código de la consulta de datos</span><span class="sxs-lookup"><span data-stu-id="705dc-253">Writing the Data Query Code</span></span>

<span data-ttu-id="705dc-254">En la parte superior de la página, entre el `@{` y `}` caracteres, escriba el código siguiente.</span><span class="sxs-lookup"><span data-stu-id="705dc-254">At the top of the page, between the `@{` and `}` characters, enter the following code.</span></span> <span data-ttu-id="705dc-255">(Asegúrese de que se escribe este código entre las llaves de cierre y apertura).</span><span class="sxs-lookup"><span data-stu-id="705dc-255">(Make sure that you enter this code between the opening and closing braces.)</span></span>

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

<span data-ttu-id="705dc-256">La primera línea abre la base de datos que creó anteriormente, que siempre es el primer paso antes de realizar alguna acción con la base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-256">The first line opens the database that you created earlier, which is always the first step before doing something with the database.</span></span> <span data-ttu-id="705dc-257">Puede indicar la `Database.Open` nombre del método de la base de datos para abrir.</span><span class="sxs-lookup"><span data-stu-id="705dc-257">You tell the `Database.Open` method name of the database to open.</span></span> <span data-ttu-id="705dc-258">Tenga en cuenta que no incluye *.sdf* en el nombre.</span><span class="sxs-lookup"><span data-stu-id="705dc-258">Notice that you don't include *.sdf* in the name.</span></span> <span data-ttu-id="705dc-259">El `Open` método supone que está buscando un *.sdf* archivo (es decir, *WebPagesMovies.sdf*) y que la *.sdf* archivo se encuentra en la *aplicación\_ Datos* carpeta.</span><span class="sxs-lookup"><span data-stu-id="705dc-259">The `Open` method assumes that it's looking for an *.sdf* file (that is, *WebPagesMovies.sdf*) and that the *.sdf* file is in the *App\_Data* folder.</span></span> <span data-ttu-id="705dc-260">(Anteriormente mencionamos que el *aplicación\_datos* carpeta está reservada; este escenario es uno de los lugares donde ASP.NET hace suposiciones sobre ese nombre.)</span><span class="sxs-lookup"><span data-stu-id="705dc-260">(Earlier we noted that the *App\_Data* folder is reserved; this scenario is one of the places where ASP.NET makes assumptions about that name.)</span></span>

<span data-ttu-id="705dc-261">Cuando se abre la base de datos, una referencia a él se coloca en la variable denominada `db`.</span><span class="sxs-lookup"><span data-stu-id="705dc-261">When the database is opened, a reference to it is put into the variable named `db`.</span></span> <span data-ttu-id="705dc-262">(Que puede tener cualquier nombre.) El `db` variable es cómo terminará interactuar con la base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-262">(Which could be named anything.) The `db` variable is how you'll end up interacting with the database.</span></span>

<span data-ttu-id="705dc-263">La segunda línea realmente captura los datos de la base de datos mediante el uso de la `Query` método.</span><span class="sxs-lookup"><span data-stu-id="705dc-263">The second line actually fetches the database data by using the `Query` method.</span></span> <span data-ttu-id="705dc-264">Observe cómo funciona este código: el `db` variable tiene una referencia a la base de datos abierto, y se invoca el `Query` método utilizando el `db` variable (`db.Query`).</span><span class="sxs-lookup"><span data-stu-id="705dc-264">Notice how this code works: the `db` variable has a reference to the opened database, and you invoke the `Query` method by using the `db` variable (`db.Query`).</span></span>

<span data-ttu-id="705dc-265">La propia consulta es una instancia de SQL `Select` instrucción.</span><span class="sxs-lookup"><span data-stu-id="705dc-265">The query itself is a SQL `Select` statement.</span></span> <span data-ttu-id="705dc-266">(Para algunos antecedentes acerca de SQL, vea la explicación más adelante). En la instrucción, `Movies` identifica la tabla a la consulta.</span><span class="sxs-lookup"><span data-stu-id="705dc-266">(For a little background about SQL, see the explanation later.) In the statement, `Movies` identifies the table to query.</span></span> <span data-ttu-id="705dc-267">El `*` carácter especifica que la consulta debe devolver todas las columnas de la tabla.</span><span class="sxs-lookup"><span data-stu-id="705dc-267">The `*` character specifies that the query should return all the columns from the table.</span></span> <span data-ttu-id="705dc-268">(También puede hacer una lista las columnas individualmente, separados por comas.)</span><span class="sxs-lookup"><span data-stu-id="705dc-268">(You could also list columns individually, separated by commas.)</span></span>

<span data-ttu-id="705dc-269">Los resultados de la consulta, si existe, se devuelve y están disponibles en la `selectedData` variable.</span><span class="sxs-lookup"><span data-stu-id="705dc-269">The results of the query, if any, are returned and made available in the `selectedData` variable.</span></span> <span data-ttu-id="705dc-270">Una vez más, la variable puede tener cualquier nombre.</span><span class="sxs-lookup"><span data-stu-id="705dc-270">Again, the variable could be named anything.</span></span>

<span data-ttu-id="705dc-271">Por último, la tercera línea indica a ASP.NET que desea usar una instancia de la `WebGrid` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="705dc-271">Finally, the third line tells ASP.NET that you want to use an instance of the `WebGrid` helper.</span></span> <span data-ttu-id="705dc-272">Crear (*crear instancias de*) el objeto auxiliar mediante el uso de la `new` palabra clave y pasar los resultados de consulta a través de la `selectedData` variable.</span><span class="sxs-lookup"><span data-stu-id="705dc-272">You create (*instantiate*) the helper object by using the `new` keyword and pass it the query results via the `selectedData` variable.</span></span> <span data-ttu-id="705dc-273">El nuevo `WebGrid` objeto, junto con los resultados de la consulta de base de datos, se ponen a disposición de los `grid` variable.</span><span class="sxs-lookup"><span data-stu-id="705dc-273">The new `WebGrid` object, along with the results of the database query, are made available in the `grid` variable.</span></span> <span data-ttu-id="705dc-274">Necesitará que den como resultado en un momento para ver los datos en la página.</span><span class="sxs-lookup"><span data-stu-id="705dc-274">You'll need that result in a moment to actually display the data in the page.</span></span>

<span data-ttu-id="705dc-275">En esta fase, se ha abierto la base de datos, ha recibido los datos que desea y que ha preparado el `WebGrid` auxiliar con esos datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-275">At this stage, the database has been opened, you've gotten the data you want, and you've prepared the `WebGrid` helper with that data.</span></span> <span data-ttu-id="705dc-276">A continuación consiste en crear el marcado en la página.</span><span class="sxs-lookup"><span data-stu-id="705dc-276">Next is to create the markup in the page.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="705dc-277">**Lenguaje de consulta estructurado (SQL)**</span><span class="sxs-lookup"><span data-stu-id="705dc-277">**Structured Query Language (SQL)**</span></span>
> 
> <span data-ttu-id="705dc-278">SQL es un lenguaje que se utiliza en la mayoría de bases de datos relacionales para administrar los datos en una base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-278">SQL is a language that's used in most relational databases for managing data in a database.</span></span> <span data-ttu-id="705dc-279">Incluye comandos que permiten recuperar datos y actualizarlo y que le permiten crear, modificar y administrar datos en tablas de base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-279">It includes commands that let you retrieve data and update it, and that let you create, modify, and manage data in database tables.</span></span> <span data-ttu-id="705dc-280">SQL es diferente de un lenguaje de programación (como C#).</span><span class="sxs-lookup"><span data-stu-id="705dc-280">SQL is different than a programming language (like C#).</span></span> <span data-ttu-id="705dc-281">Con SQL, puede indicar a la base de datos de la acción que realizará y es trabajo de la base de datos para averiguar cómo obtener los datos o realizar la tarea.</span><span class="sxs-lookup"><span data-stu-id="705dc-281">With SQL, you tell the database what you want, and it's the database's job to figure out how to get the data or perform the task.</span></span> <span data-ttu-id="705dc-282">Estos son algunos ejemplos de algunos comandos SQL y qué hacer:</span><span class="sxs-lookup"><span data-stu-id="705dc-282">Here are examples of some SQL commands and what they do:</span></span>
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> <span data-ttu-id="705dc-283">La primera `Select` instrucción obtiene todas las columnas (especificado por `*`) desde el *películas* tabla.</span><span class="sxs-lookup"><span data-stu-id="705dc-283">The first `Select` statement gets all the columns (specified by `*`) from the *Movies* table.</span></span>
> 
> <span data-ttu-id="705dc-284">El segundo `Select` instrucción captura las columnas Id., el nombre y el precio de registros en la *producto* tabla cuyo valor de columna precio es superior a 10.</span><span class="sxs-lookup"><span data-stu-id="705dc-284">The second `Select` statement fetches the ID, Name, and Price columns from records in the *Product* table whose Price column value is more than 10.</span></span> <span data-ttu-id="705dc-285">El comando devuelve los resultados en orden alfabético según los valores de la columna nombre.</span><span class="sxs-lookup"><span data-stu-id="705dc-285">The command returns the results in alphabetical order based on the values of the Name column.</span></span> <span data-ttu-id="705dc-286">Si no hay registros que coincidan con los criterios de precio, el comando devuelve un conjunto vacío.</span><span class="sxs-lookup"><span data-stu-id="705dc-286">If no records match the price criteria, the command returns an empty set.</span></span>
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> <span data-ttu-id="705dc-287">Este comando inserta un nuevo registro en el *producto* tabla, establecer la columna de nombre "Croissant", la columna Description para "Alegría de inestable A" y el precio en 1.99.</span><span class="sxs-lookup"><span data-stu-id="705dc-287">This command inserts a new record into the *Product* table, setting the Name column to "Croissant", the Description column to "A flaky delight", and the price to 1.99.</span></span>
> 
> <span data-ttu-id="705dc-288">Tenga en cuenta que cuando está especificando un valor no numérico, el valor se encierra entre comillas simples (no comillas dobles, como en C#).</span><span class="sxs-lookup"><span data-stu-id="705dc-288">Notice that when you're specifying a non-numeric value, the value is enclosed in single quotation marks (not double quotation marks, as in C#).</span></span> <span data-ttu-id="705dc-289">Use estas comillas alrededor de los valores de texto o de fecha, pero no en torno a números.</span><span class="sxs-lookup"><span data-stu-id="705dc-289">You use these quotation marks around text or date values, but not around numbers.</span></span>
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> <span data-ttu-id="705dc-290">Este comando elimina los registros de la *producto* tabla cuya columna de fecha de expiración es anterior al 1 de enero de 2008.</span><span class="sxs-lookup"><span data-stu-id="705dc-290">This command deletes records in the *Product* table whose expiration date column is earlier than January 1, 2008.</span></span> <span data-ttu-id="705dc-291">(El comando supone que la *producto* tabla tiene una columna de este tipo, por supuesto.) Se especifica la fecha en formato MM/DD/AAAA, pero debe especificarse en el formato que se usa para la configuración regional.</span><span class="sxs-lookup"><span data-stu-id="705dc-291">(The command assumes that the *Product* table has such a column, of course.) The date is entered here in MM/DD/YYYY format, but it should be entered in the format that's used for your locale.</span></span>
> 
> <span data-ttu-id="705dc-292">El `Insert` y `Delete` comandos no devuelven conjuntos de resultados.</span><span class="sxs-lookup"><span data-stu-id="705dc-292">The `Insert` and `Delete` commands don't return result sets.</span></span> <span data-ttu-id="705dc-293">En su lugar, devuelven un número que indica el número de registros se vieron afectado por el comando.</span><span class="sxs-lookup"><span data-stu-id="705dc-293">Instead, they return a number that tells you how many records were affected by the command.</span></span>
> 
> <span data-ttu-id="705dc-294">Para algunas de estas operaciones (por ejemplo, insertar y eliminar registros), el proceso que está solicitando la operación debe tener los permisos adecuados en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-294">For some of these operations (like inserting and deleting records), the process that's requesting the operation has to have appropriate permissions in the database.</span></span> <span data-ttu-id="705dc-295">Que es la razón para bases de datos de producción a menudo tendrá que proporcionar un nombre de usuario y una contraseña cuando se conecta a la base de datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-295">That's why for production databases you often have to supply a user name and password when you connect to the database.</span></span>
> 
> <span data-ttu-id="705dc-296">Existen docenas de comandos SQL, pero siguen un patrón como los comandos que ve aquí.</span><span class="sxs-lookup"><span data-stu-id="705dc-296">There are dozens of SQL commands, but they all follow a pattern like the commands you see here.</span></span> <span data-ttu-id="705dc-297">Puede utilizar comandos SQL para crear tablas de base de datos, contar el número de registros de una tabla, calcular precios y realizar muchas más operaciones.</span><span class="sxs-lookup"><span data-stu-id="705dc-297">You can use SQL commands to create database tables, count the number of records in a table, calculate prices, and perform many more operations.</span></span>


### <a name="adding-markup-to-display-the-data"></a><span data-ttu-id="705dc-298">Agregar marcado para mostrar los datos</span><span class="sxs-lookup"><span data-stu-id="705dc-298">Adding Markup to Display the Data</span></span>

<span data-ttu-id="705dc-299">Dentro de la `<head>` elemento, conjunto de contenido de la `<title>` elemento a "Películas":</span><span class="sxs-lookup"><span data-stu-id="705dc-299">Inside the `<head>` element, set contents of the `<title>` element to "Movies":</span></span>

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

<span data-ttu-id="705dc-300">Dentro de la `<body>` elemento de la página, agregue lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="705dc-300">Inside the `<body>` element of the page, add the following:</span></span>

[!code-html[Main](displaying-data/samples/sample3.html)]

<span data-ttu-id="705dc-301">Ya está.</span><span class="sxs-lookup"><span data-stu-id="705dc-301">That's it.</span></span> <span data-ttu-id="705dc-302">El `grid` variable es el valor que se creó al crear la `WebGrid` objeto en el código anterior.</span><span class="sxs-lookup"><span data-stu-id="705dc-302">The `grid` variable is the value you created when you created the `WebGrid` object in code earlier.</span></span>

<span data-ttu-id="705dc-303">En la vista de árbol de WebMatrix, haga clic en la página y seleccione **iniciar en un explorador**.</span><span class="sxs-lookup"><span data-stu-id="705dc-303">In the WebMatrix tree view, right-click the page and select **Launch in browser**.</span></span> <span data-ttu-id="705dc-304">Verá algo parecido a esta página:</span><span class="sxs-lookup"><span data-stu-id="705dc-304">You'll see something like this page:</span></span>

![Salida de aplicación auxiliar WebGrid predeterminada de la tabla de películas](displaying-data/_static/image18.png)

<span data-ttu-id="705dc-306">Haga clic en un vínculo de encabezado de columna para ordenar por esa columna.</span><span class="sxs-lookup"><span data-stu-id="705dc-306">Click a column heading link to sort by that column.</span></span> <span data-ttu-id="705dc-307">Posibilidad de ordenar haciendo clic en un encabezado es una característica que está integrada en el **WebGrid** auxiliar.</span><span class="sxs-lookup"><span data-stu-id="705dc-307">Being able to sort by clicking a heading is a feature that's built into the **WebGrid** helper.</span></span>

<span data-ttu-id="705dc-308">El `GetHtml` método, tal y como sugiere su nombre, genera marcado que muestra los datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-308">The `GetHtml` method, as its name suggests, generates markup that displays the data.</span></span> <span data-ttu-id="705dc-309">De forma predeterminada, el `GetHtml` método genera HTML `<table>` elemento.</span><span class="sxs-lookup"><span data-stu-id="705dc-309">By default, the `GetHtml` method generates an HTML `<table>` element.</span></span> <span data-ttu-id="705dc-310">(Si lo desea, puede comprobar la representación mediante la visualización en el origen de la página en el explorador.)</span><span class="sxs-lookup"><span data-stu-id="705dc-310">(If you want, you can verify the rendering by looking at the source of the page in the browser.)</span></span>

## <a name="modifying-the-look-of-the-grid"></a><span data-ttu-id="705dc-311">Modificar la apariencia de la cuadrícula</span><span class="sxs-lookup"><span data-stu-id="705dc-311">Modifying the Look of the Grid</span></span>

<span data-ttu-id="705dc-312">Mediante el `WebGrid` auxiliar como hizo simplemente es fácil, pero la presentación resultante es sencillo.</span><span class="sxs-lookup"><span data-stu-id="705dc-312">Using the `WebGrid` helper like you just did is easy, but the resulting display is plain.</span></span> <span data-ttu-id="705dc-313">El `WebGrid` auxiliar tiene todo tipo de opciones que permiten controlar cómo se muestran los datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-313">The `WebGrid` helper has all sorts of options that let you control how the data is displayed.</span></span> <span data-ttu-id="705dc-314">Hay demasiadas para explorar en este tutorial, pero en esta sección le dará una idea de algunas de estas opciones.</span><span class="sxs-lookup"><span data-stu-id="705dc-314">There are far too many to explore in this tutorial, but this section will give you an idea of some of those options.</span></span> <span data-ttu-id="705dc-315">Algunas opciones adicionales se tratará en tutoriales posteriores de esta serie.</span><span class="sxs-lookup"><span data-stu-id="705dc-315">A few additional options will be covered in later tutorials in this series.</span></span>

### <a name="specifying-individual-columns-to-display"></a><span data-ttu-id="705dc-316">Especificar columnas individuales para mostrar</span><span class="sxs-lookup"><span data-stu-id="705dc-316">Specifying Individual Columns to Display</span></span>

<span data-ttu-id="705dc-317">Para empezar, puede especificar que desea mostrar solo ciertas columnas.</span><span class="sxs-lookup"><span data-stu-id="705dc-317">To start, you can specify that you want to display only certain columns.</span></span> <span data-ttu-id="705dc-318">De forma predeterminada, tal y como se ha visto, la cuadrícula muestra los cuatro de las columnas de la *películas* tabla.</span><span class="sxs-lookup"><span data-stu-id="705dc-318">By default, as you've seen, the grid shows all four of the columns from the *Movies* table.</span></span>

<span data-ttu-id="705dc-319">En el *Movies.cshtml* archivo, reemplace el `@grid.GetHtml()` marcado que acaba de agregar por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="705dc-319">In the *Movies.cshtml* file, replace the `@grid.GetHtml()` markup that you just added with the following:</span></span>

[!code-css[Main](displaying-data/samples/sample4.css)]

<span data-ttu-id="705dc-320">Para indicar qué columnas desea mostrar de la aplicación auxiliar, incluye un `columns` parámetro para el `GetHtml` método y pasar de una colección de columnas.</span><span class="sxs-lookup"><span data-stu-id="705dc-320">To tell the helper which columns to display, you include a `columns` parameter for the `GetHtml` method and pass in a collection of columns.</span></span> <span data-ttu-id="705dc-321">En la colección, puede especificar cada columna que se va a incluir.</span><span class="sxs-lookup"><span data-stu-id="705dc-321">In the collection, you specify each column to include.</span></span> <span data-ttu-id="705dc-322">Especificar una columna individual para mostrar mediante la inclusión de un `grid.Column` de objetos y pasa el nombre de la columna de datos que desea.</span><span class="sxs-lookup"><span data-stu-id="705dc-322">You specify an individual column to display by including a `grid.Column` object, and pass in the name of the data column you want.</span></span> <span data-ttu-id="705dc-323">(Estas columnas deben incluirse en los resultados de la consulta SQL: el `WebGrid` auxiliar no puede mostrar las columnas que no se han devuelto por la consulta.)</span><span class="sxs-lookup"><span data-stu-id="705dc-323">(These columns must be included in the SQL query results — the `WebGrid` helper cannot display columns that were not returned by the query.)</span></span>

<span data-ttu-id="705dc-324">Iniciar el *Movies.cshtml* página en el explorador y esta vez llegue una pantalla como la siguiente (Observe que no se muestra ninguna columna de Id.):</span><span class="sxs-lookup"><span data-stu-id="705dc-324">Launch the *Movies.cshtml* page in the browser again, and this time you get a display like the following one (notice that no ID column is displayed):</span></span>

![WebGrid que muestre sólo las columnas seleccionadas](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a><span data-ttu-id="705dc-326">Cambiar la apariencia de la cuadrícula</span><span class="sxs-lookup"><span data-stu-id="705dc-326">Changing the Look of the Grid</span></span>

<span data-ttu-id="705dc-327">Hay bastantes más opciones para mostrar columnas, algunos de los cuales se explorarán en tutoriales más adelante en este conjunto.</span><span class="sxs-lookup"><span data-stu-id="705dc-327">There are quite a few more options for displaying columns, some of which will be explored in later tutorials in this set.</span></span> <span data-ttu-id="705dc-328">Por ahora, esta sección describe a formas en que puede aplicar el estilo de la cuadrícula como un todo.</span><span class="sxs-lookup"><span data-stu-id="705dc-328">For now, this section will introduce you to ways in which you can style the grid as a whole.</span></span>

<span data-ttu-id="705dc-329">Dentro de la `<head>` sección de la página, justo antes del cierre `</head>` etiqueta, agregue las siguientes `<style>` elemento:</span><span class="sxs-lookup"><span data-stu-id="705dc-329">Inside the `<head>` section of the page, just before the closing `</head>` tag, add the following `<style>` element:</span></span>

[!code-css[Main](displaying-data/samples/sample5.css)]

<span data-ttu-id="705dc-330">Este marcado CSS define clases denominadas `grid`, `head`, y así sucesivamente.</span><span class="sxs-lookup"><span data-stu-id="705dc-330">This CSS markup defines classes named `grid`, `head`, and so on.</span></span> <span data-ttu-id="705dc-331">También puede colocar estas definiciones de estilo en otro *.css* de archivos y vincularla a la página.</span><span class="sxs-lookup"><span data-stu-id="705dc-331">You could also put these style definitions in a separate *.css* file and link that to the page.</span></span> <span data-ttu-id="705dc-332">(De hecho, llevará a cabo más adelante en este tutorial conjunto.) Pero para facilitar la cosas para este tutorial, se usan dentro de la misma página que muestra los datos.</span><span class="sxs-lookup"><span data-stu-id="705dc-332">(In fact, you'll do that later in this tutorial set.) But to make things easy for this tutorial, they're inside the same page that displays the data.</span></span>

<span data-ttu-id="705dc-333">Ahora puede obtener el `WebGrid` auxiliar para utilizar estas clases de estilo.</span><span class="sxs-lookup"><span data-stu-id="705dc-333">Now you can get the `WebGrid` helper to use these style classes.</span></span> <span data-ttu-id="705dc-334">La aplicación auxiliar tiene un número de propiedades (por ejemplo, `tableStyle`) para este fin, se asigna un nombre de clase de estilo CSS a ellos, y ese nombre de clase se representa como parte del marcado que representa la aplicación auxiliar.</span><span class="sxs-lookup"><span data-stu-id="705dc-334">The helper has a number of properties (for example, `tableStyle`) for just this purpose — you assign a CSS style class name to them, and that class name is rendered as part of the markup that's rendered by the helper.</span></span>

<span data-ttu-id="705dc-335">Cambiar el `grid.GetHtml` marcado para ese y ahora como se muestra en este código:</span><span class="sxs-lookup"><span data-stu-id="705dc-335">Change the `grid.GetHtml` markup so that it now looks like this code:</span></span>

[!code-css[Main](displaying-data/samples/sample6.css)]

<span data-ttu-id="705dc-336">' S new aquí es que se ha agregado `tableStyle`, `headerStyle`, y `alternatingRowStyle` parámetros para el `GetHtml` método.</span><span class="sxs-lookup"><span data-stu-id="705dc-336">What's new here is that you've added `tableStyle`, `headerStyle`, and `alternatingRowStyle` parameters to the `GetHtml` method.</span></span> <span data-ttu-id="705dc-337">Estos parámetros se encuentran en los nombres de los estilos CSS que agregó hace un momento.</span><span class="sxs-lookup"><span data-stu-id="705dc-337">These parameters have been set to the names of the CSS styles that you added a moment ago.</span></span>

<span data-ttu-id="705dc-338">Ejecute la página, y esta ocasión verá una cuadrícula que se parece mucho más sencillo que antes:</span><span class="sxs-lookup"><span data-stu-id="705dc-338">Run the page, and this time you see a grid that looks much less plain than before:</span></span>

![Pantalla de WebGrid que incluye los parámetros establecidos en los nombres de clase CSS](displaying-data/_static/image20.png)

<span data-ttu-id="705dc-340">Para ver qué la `GetHtml` método generado, puede buscar en el origen de la página en el explorador.</span><span class="sxs-lookup"><span data-stu-id="705dc-340">To see what the `GetHtml` method generated, you can look at the source of the page in the browser.</span></span> <span data-ttu-id="705dc-341">No entraremos en detalles aquí, pero lo importante es que al especificar parámetros como `tableStyle`, provocó la cuadrícula generar etiquetas HTML similar al siguiente:</span><span class="sxs-lookup"><span data-stu-id="705dc-341">We won't go into detail here, but the important point is that by specifying parameters like `tableStyle`, you caused the grid to generate HTML tags like the following:</span></span>

`<table class="grid">`

<span data-ttu-id="705dc-342">El `<table>` etiqueta ha tenido un `class` atributo agregado a lo que hace referencia a una de las reglas CSS que agregó anteriormente.</span><span class="sxs-lookup"><span data-stu-id="705dc-342">The `<table>` tag has had a `class` attribute added to it that references one of the CSS rules that you added earlier.</span></span> <span data-ttu-id="705dc-343">Este código muestra el patrón básico &mdash; parámetros diferentes para el `GetHtml` método permiten hacer referencia CSS clases que el método genera, a continuación, junto con el marcado.</span><span class="sxs-lookup"><span data-stu-id="705dc-343">This code shows you the basic pattern &mdash; different parameters for the `GetHtml` method let you reference CSS classes that the method then generates along with the markup.</span></span> <span data-ttu-id="705dc-344">Qué hacer con las clases CSS depende de usted.</span><span class="sxs-lookup"><span data-stu-id="705dc-344">What you do with the CSS classes is up to you.</span></span>

## <a name="adding-paging"></a><span data-ttu-id="705dc-345">Agregar paginación</span><span class="sxs-lookup"><span data-stu-id="705dc-345">Adding Paging</span></span>

<span data-ttu-id="705dc-346">Como la última tarea para este tutorial, agregará la paginación a la cuadrícula.</span><span class="sxs-lookup"><span data-stu-id="705dc-346">As the last task for this tutorial, you'll add paging to the grid.</span></span> <span data-ttu-id="705dc-347">En estos momentos no es ningún problema para mostrar todas las películas a la vez.</span><span class="sxs-lookup"><span data-stu-id="705dc-347">Right now it's no problem to display all your movies at once.</span></span> <span data-ttu-id="705dc-348">Pero si agrega cientos de películas, la presentación de página obtendría largo.</span><span class="sxs-lookup"><span data-stu-id="705dc-348">But if you added hundreds of movies, the page display would get long.</span></span>

<span data-ttu-id="705dc-349">En el código de la página, cambie la línea que crea la `WebGrid` objeto en el código siguiente:</span><span class="sxs-lookup"><span data-stu-id="705dc-349">In the page code, change the line that creates the `WebGrid` object to the following code:</span></span>

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

<span data-ttu-id="705dc-350">La única diferencia de antes es que se ha agregado un `rowsPerPage` parámetro que se establece en 3.</span><span class="sxs-lookup"><span data-stu-id="705dc-350">The only difference from before is that you've added a `rowsPerPage` parameter that's set to 3.</span></span>

<span data-ttu-id="705dc-351">Ejecute la página.</span><span class="sxs-lookup"><span data-stu-id="705dc-351">Run the page.</span></span> <span data-ttu-id="705dc-352">La cuadrícula muestra 3 filas en una hora más vínculos de navegación que permiten hojear las películas en la base de datos:</span><span class="sxs-lookup"><span data-stu-id="705dc-352">The grid displays 3 rows at a time, plus navigation links that let you page through the movies in your database:</span></span>

![Mostrar WebGrid con paginación](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a><span data-ttu-id="705dc-354">Próxima</span><span class="sxs-lookup"><span data-stu-id="705dc-354">Coming Up Next</span></span>

<span data-ttu-id="705dc-355">En el siguiente tutorial, aprenderá cómo utilizar código Razor y C# para obtener proporcionados por el usuario en un formulario.</span><span class="sxs-lookup"><span data-stu-id="705dc-355">In the next tutorial, you'll learn how to use Razor and C# code to get user input in a form.</span></span> <span data-ttu-id="705dc-356">Para que puedan encontrar películas por título o género, agregará un cuadro de búsqueda a la página de películas.</span><span class="sxs-lookup"><span data-stu-id="705dc-356">You'll add a search box to the Movies page so that you can find movies by title or genre.</span></span>

## <a name="complete-listing-for-movies-page"></a><span data-ttu-id="705dc-357">Muestra la lista completa de la página de películas</span><span class="sxs-lookup"><span data-stu-id="705dc-357">Complete Listing for Movies Page</span></span>

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="705dc-358">Recursos adicionales</span><span class="sxs-lookup"><span data-stu-id="705dc-358">Additional Resources</span></span>

- [<span data-ttu-id="705dc-359">Introducción a la programación Web de ASP.NET mediante la sintaxis Razor</span><span class="sxs-lookup"><span data-stu-id="705dc-359">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)

>[!div class="step-by-step"]
<span data-ttu-id="705dc-360">[Anterior](intro-to-web-pages-programming.md)
[Siguiente](form-basics.md)</span><span class="sxs-lookup"><span data-stu-id="705dc-360">[Previous](intro-to-web-pages-programming.md)
[Next](form-basics.md)</span></span>
