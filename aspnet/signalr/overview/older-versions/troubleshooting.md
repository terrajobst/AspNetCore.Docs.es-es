---
uid: signalr/overview/older-versions/troubleshooting
title: Solución de problemas de SignalR (SignalR 1.x) | Documentos de Microsoft
author: pfletcher
description: Este artículo describen problemas comunes con el desarrollo de aplicaciones SignalR.
ms.author: aspnetcontent
manager: wpickett
ms.date: 06/05/2013
ms.topic: article
ms.assetid: 347210ba-c452-4feb-886f-b51d89f58971
ms.technology: dotnet-signalr
ms.prod: .net-framework
msc.legacyurl: /signalr/overview/older-versions/troubleshooting
msc.type: authoredcontent
ms.openlocfilehash: eb739f806eb57d09008fa0bf04b5a40721dab73d
ms.sourcegitcommit: 9a9483aceb34591c97451997036a9120c3fe2baf
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 11/10/2017
---
<a name="signalr-troubleshooting-signalr-1x"></a><span data-ttu-id="ab18e-103">Solución de problemas de SignalR (SignalR 1.x)</span><span class="sxs-lookup"><span data-stu-id="ab18e-103">SignalR Troubleshooting (SignalR 1.x)</span></span>
====================
<span data-ttu-id="ab18e-104">por [Patrick Fletcher](https://github.com/pfletcher)</span><span class="sxs-lookup"><span data-stu-id="ab18e-104">by [Patrick Fletcher](https://github.com/pfletcher)</span></span>

> <span data-ttu-id="ab18e-105">Este documento describen los problemas más comunes con SignalR.</span><span class="sxs-lookup"><span data-stu-id="ab18e-105">This document describes common troubleshooting issues with SignalR.</span></span>


<span data-ttu-id="ab18e-106">Este documento contiene las siguientes secciones.</span><span class="sxs-lookup"><span data-stu-id="ab18e-106">This document contains the following sections.</span></span>

- [<span data-ttu-id="ab18e-107">Llamar a métodos entre el cliente y el servidor en modo silencioso se produce un error</span><span class="sxs-lookup"><span data-stu-id="ab18e-107">Calling methods between the client and server silently fails</span></span>](#connection)
- [<span data-ttu-id="ab18e-108">Otros problemas de conexión</span><span class="sxs-lookup"><span data-stu-id="ab18e-108">Other connection issues</span></span>](#other)
- [<span data-ttu-id="ab18e-109">Errores de compilación y el lado servidor</span><span class="sxs-lookup"><span data-stu-id="ab18e-109">Compilation and server-side errors</span></span>](#server)
- [<span data-ttu-id="ab18e-110">Problemas de Visual Studio</span><span class="sxs-lookup"><span data-stu-id="ab18e-110">Visual Studio issues</span></span>](#vs)
- [<span data-ttu-id="ab18e-111">Problemas de Internet Information Services</span><span class="sxs-lookup"><span data-stu-id="ab18e-111">Internet Information Services issues</span></span>](#iis)
- [<span data-ttu-id="ab18e-112">Problemas de Azure</span><span class="sxs-lookup"><span data-stu-id="ab18e-112">Azure issues</span></span>](#azure)

<a id="connection"></a>

## <a name="calling-methods-between-the-client-and-server-silently-fails"></a><span data-ttu-id="ab18e-113">Llamar a métodos entre el cliente y el servidor en modo silencioso se produce un error</span><span class="sxs-lookup"><span data-stu-id="ab18e-113">Calling methods between the client and server silently fails</span></span>

<span data-ttu-id="ab18e-114">Esta sección describen las posibles causas de una llamada al método entre cliente y servidor para producir un error sin un mensaje de error significativo.</span><span class="sxs-lookup"><span data-stu-id="ab18e-114">This section describes possible causes for a method call between client and server to fail without a meaningful error message.</span></span> <span data-ttu-id="ab18e-115">En una aplicación de SignalR, el servidor no tiene información sobre los métodos que implementa el cliente; Cuando el servidor, invoca un método de cliente, los datos de nombre y los parámetros de método se envían al cliente y el método se ejecuta sólo si se encuentra en el formato que el servidor especificado.</span><span class="sxs-lookup"><span data-stu-id="ab18e-115">In a SignalR application, the server has no information about the methods that the client implements; when the server invokes a client method, the method name and parameter data are sent to the client, and the method is executed only if it exists in the format that the server specified.</span></span> <span data-ttu-id="ab18e-116">Si no se encuentra ningún método coincidente en el cliente, no ocurre nada, y no se genera ningún mensaje de error en el servidor.</span><span class="sxs-lookup"><span data-stu-id="ab18e-116">If no matching method is found on the client, nothing happens, and no error message is raised on the server.</span></span>

<span data-ttu-id="ab18e-117">Para investigar más sobre los métodos de cliente no esté llamando, puede activar el registro antes de llamar al método start en el concentrador para ver qué llamadas se procedente del servidor.</span><span class="sxs-lookup"><span data-stu-id="ab18e-117">To further investigate client methods not getting called, you can turn on logging before the calling the start method on the hub to see what calls are coming from the server.</span></span> <span data-ttu-id="ab18e-118">Para habilitar el registro en una aplicación de JavaScript, consulte [cómo habilitar el registro de cliente (versión de cliente de JavaScript)](../guide-to-the-api/hubs-api-guide-javascript-client.md#logging).</span><span class="sxs-lookup"><span data-stu-id="ab18e-118">To enable logging in a JavaScript application, see [How to enable client-side logging (JavaScript client version)](../guide-to-the-api/hubs-api-guide-javascript-client.md#logging).</span></span> <span data-ttu-id="ab18e-119">Para habilitar el registro en una aplicación de cliente. NET, vea [cómo habilitar el registro de cliente (versión de cliente. NET)](../guide-to-the-api/hubs-api-guide-net-client.md#logging).</span><span class="sxs-lookup"><span data-stu-id="ab18e-119">To enable logging in a .NET client application, see [How to enable client-side logging (.NET Client version)](../guide-to-the-api/hubs-api-guide-net-client.md#logging).</span></span>

### <a name="misspelled-method-incorrect-method-signature-or-incorrect-hub-name"></a><span data-ttu-id="ab18e-120">Método mal escrito, firma de método incorrecto o nombre del concentrador incorrecto</span><span class="sxs-lookup"><span data-stu-id="ab18e-120">Misspelled method, incorrect method signature, or incorrect hub name</span></span>

<span data-ttu-id="ab18e-121">Si el nombre o la firma de un método llamado coincide exactamente un método adecuado en el cliente, se producirá un error en la llamada.</span><span class="sxs-lookup"><span data-stu-id="ab18e-121">If the name or signature of a called method does not exactly match an appropriate method on the client, the call will fail.</span></span> <span data-ttu-id="ab18e-122">Compruebe que el nombre del método llamado por el servidor coincide con el nombre del método en el cliente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-122">Verify that the method name called by the server matches the name of the method on the client.</span></span> <span data-ttu-id="ab18e-123">Además, SignalR crea el proxy de concentrador mediante métodos de grafía de camel, según resulte adecuado en JavaScript, por lo que un método llamado `SendMessage` en el servidor se denominaría `sendMessage` en el proxy de cliente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-123">Also, SignalR creates the hub proxy using camel-cased methods, as is appropriate in JavaScript, so a method called `SendMessage` on the server would be called `sendMessage` in the client proxy.</span></span> <span data-ttu-id="ab18e-124">Si usas el `HubName` de atributo en el código de servidor, compruebe que el nombre utilizado coincide con el nombre utilizado para crear el concentrador en el cliente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-124">If you use the `HubName` attribute in your server-side code, verify that the name used matches the name used to create the hub on the client.</span></span> <span data-ttu-id="ab18e-125">Si no usa el `HubName` atributo, compruebe que el nombre del concentrador en un cliente de JavaScript es la grafía camel, por ejemplo, chatHub en lugar de ChatHub.</span><span class="sxs-lookup"><span data-stu-id="ab18e-125">If you do not use the `HubName` attribute, verify that the name of the hub in a JavaScript client is camel-cased, such as chatHub instead of ChatHub.</span></span>

### <a name="duplicate-method-name-on-client"></a><span data-ttu-id="ab18e-126">Nombre de método en el cliente está duplicado</span><span class="sxs-lookup"><span data-stu-id="ab18e-126">Duplicate method name on client</span></span>

<span data-ttu-id="ab18e-127">Compruebe que no tiene un método duplicado en el cliente que se diferencia sólo por mayúsculas o minúsculas.</span><span class="sxs-lookup"><span data-stu-id="ab18e-127">Verify that you do not have a duplicate method on the client that differs only by case.</span></span> <span data-ttu-id="ab18e-128">Si la aplicación cliente tiene un método denominado `sendMessage`, asegúrese de que también hay un método llamado `SendMessage` así.</span><span class="sxs-lookup"><span data-stu-id="ab18e-128">If your client application has a method called `sendMessage`, verify that there isn't also a method called `SendMessage` as well.</span></span>

### <a name="missing-json-parser-on-the-client"></a><span data-ttu-id="ab18e-129">Analizador JSON que falta en el cliente</span><span class="sxs-lookup"><span data-stu-id="ab18e-129">Missing JSON parser on the client</span></span>

<span data-ttu-id="ab18e-130">SignalR requiere un analizador JSON que esté presente para serializar las llamadas entre el servidor y el cliente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-130">SignalR requires a JSON parser to be present to serialize calls between the server and the client.</span></span> <span data-ttu-id="ab18e-131">Si el cliente no tiene un analizador JSON integrado (por ejemplo, Internet Explorer 7), debe incluir en la aplicación.</span><span class="sxs-lookup"><span data-stu-id="ab18e-131">If your client doesn't have a built-in JSON parser (such as Internet Explorer 7), you'll need to include one in your application.</span></span> <span data-ttu-id="ab18e-132">Puede descargar el analizador JSON [aquí](http://nuget.org/packages/json2).</span><span class="sxs-lookup"><span data-stu-id="ab18e-132">You can download the JSON parser [here](http://nuget.org/packages/json2).</span></span>

### <a name="mixing-hub-and-persistentconnection-syntax"></a><span data-ttu-id="ab18e-133">Mezclar la sintaxis de tipo Hub- and PersistentConnection</span><span class="sxs-lookup"><span data-stu-id="ab18e-133">Mixing Hub and PersistentConnection syntax</span></span>

<span data-ttu-id="ab18e-134">SignalR usa dos modelos de comunicación: concentradores y PersistentConnections.</span><span class="sxs-lookup"><span data-stu-id="ab18e-134">SignalR uses two communication models: Hubs and PersistentConnections.</span></span> <span data-ttu-id="ab18e-135">La sintaxis para llamar a estos modelos de dos comunicación es diferente en el código de cliente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-135">The syntax for calling these two communication models is different in the client code.</span></span> <span data-ttu-id="ab18e-136">Si ha agregado un concentrador en el código del servidor, compruebe que todo el código de cliente utiliza la sintaxis correcta de concentrador.</span><span class="sxs-lookup"><span data-stu-id="ab18e-136">If you have added a hub in your server code, verify that all of your client code uses the proper hub syntax.</span></span>

<span data-ttu-id="ab18e-137">**Código de cliente de JavaScript que crea una PersistentConnection en un cliente de JavaScript**</span><span class="sxs-lookup"><span data-stu-id="ab18e-137">**JavaScript client code that creates a PersistentConnection in a JavaScript client**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample1.js)]

<span data-ttu-id="ab18e-138">**Código de cliente de JavaScript que crea a un Proxy de concentrador en un cliente de Javascript**</span><span class="sxs-lookup"><span data-stu-id="ab18e-138">**JavaScript client code that creates a Hub Proxy in a Javascript client**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample2.js)]

<span data-ttu-id="ab18e-139">**Código de servidor de C# que se asigna una ruta a un PersistentConnection**</span><span class="sxs-lookup"><span data-stu-id="ab18e-139">**C# server code that maps a route to a PersistentConnection**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample3.cs)]

<span data-ttu-id="ab18e-140">**Código de servidor de C# que se asigna una ruta a un concentrador o a los centros de múltiples si tiene varias aplicaciones**</span><span class="sxs-lookup"><span data-stu-id="ab18e-140">**C# server code that maps a route to a Hub, or to mulitple hubs if you have multiple applications**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample4.cs)]

### <a name="connection-started-before-subscriptions-are-added"></a><span data-ttu-id="ab18e-141">Conexión que se inicia antes de que se agregan las suscripciones</span><span class="sxs-lookup"><span data-stu-id="ab18e-141">Connection started before subscriptions are added</span></span>

<span data-ttu-id="ab18e-142">Si se inicia conexión del concentrador antes de que los métodos que se pueda llamar desde el servidor se agregan al proxy, no se recibirán los mensajes.</span><span class="sxs-lookup"><span data-stu-id="ab18e-142">If the Hub's connection is started before methods that can be called from the server are added to the proxy, messages will not be received.</span></span> <span data-ttu-id="ab18e-143">El siguiente código de JavaScript no iniciará el concentrador correctamente:</span><span class="sxs-lookup"><span data-stu-id="ab18e-143">The following JavaScript code will not start the hub properly:</span></span>

<span data-ttu-id="ab18e-144">**Código de cliente de JavaScript incorrecta que no permitirá que van a recibir los mensajes de concentradores**</span><span class="sxs-lookup"><span data-stu-id="ab18e-144">**Incorrect JavaScript client code that will not allow Hubs messages to be received**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample5.js)]

<span data-ttu-id="ab18e-145">En su lugar, agregue las suscripciones del método antes de llamar a Start:</span><span class="sxs-lookup"><span data-stu-id="ab18e-145">Instead, add the method subscriptions before calling Start:</span></span>

<span data-ttu-id="ab18e-146">**Código de cliente de JavaScript que se agrega correctamente las suscripciones a un concentrador**</span><span class="sxs-lookup"><span data-stu-id="ab18e-146">**JavaScript client code that correctly adds subscriptions to a hub**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample6.js)]

### <a name="missing-method-name-on-the-hub-proxy"></a><span data-ttu-id="ab18e-147">Falta el nombre de método en el proxy de concentrador</span><span class="sxs-lookup"><span data-stu-id="ab18e-147">Missing method name on the hub proxy</span></span>

<span data-ttu-id="ab18e-148">Compruebe que el método definido en el servidor se ha suscrito en el cliente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-148">Verify that the method defined on the server is subscribed to on the client.</span></span> <span data-ttu-id="ab18e-149">Aunque el servidor define el método, todavía debe agregarse al proxy de cliente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-149">Even though the server defines the method, it must still be added to the client proxy.</span></span> <span data-ttu-id="ab18e-150">Métodos pueden agregarse para el proxy de cliente de las maneras siguientes (tenga en cuenta que el método se agrega a la `client` miembro del concentrador, no directamente el concentrador):</span><span class="sxs-lookup"><span data-stu-id="ab18e-150">Methods can be added to the client proxy in the following ways (Note that the method is added to the `client` member of the hub, not the hub directly):</span></span>

<span data-ttu-id="ab18e-151">**Código de cliente de JavaScript que se agrega los métodos a un proxy de concentrador**</span><span class="sxs-lookup"><span data-stu-id="ab18e-151">**JavaScript client code that adds methods to a hub proxy**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample7.js)]

### <a name="hub-or-hub-methods-not-declared-as-public"></a><span data-ttu-id="ab18e-152">Concentrador o métodos de concentrador no declarados como público</span><span class="sxs-lookup"><span data-stu-id="ab18e-152">Hub or hub methods not declared as Public</span></span>

<span data-ttu-id="ab18e-153">Para que esté visible en el cliente, la implementación del concentrador y métodos deben declararse como `public`.</span><span class="sxs-lookup"><span data-stu-id="ab18e-153">To be visible on the client, the hub implementation and methods must be declared as `public`.</span></span>

### <a name="accessing-hub-from-a-different-application"></a><span data-ttu-id="ab18e-154">Al obtener acceso al centro de la otra aplicación</span><span class="sxs-lookup"><span data-stu-id="ab18e-154">Accessing hub from a different application</span></span>

<span data-ttu-id="ab18e-155">Concentradores de SignalR sólo puede tener acceso a través de las aplicaciones que implementan a los clientes de SignalR.</span><span class="sxs-lookup"><span data-stu-id="ab18e-155">SignalR Hubs can only be accessed through applications that implement SignalR clients.</span></span> <span data-ttu-id="ab18e-156">SignalR no puede interoperar con otras bibliotecas de comunicación (por ejemplo, SOAP o WCF servicios web.) Si no hay ningún cliente de SignalR disponible para la plataforma de destino, no se puede tener acceso a punto de conexión del servidor directamente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-156">SignalR cannot interoperate with other communication libraries (like SOAP or WCF web services.) If there is no SignalR client available for your target platform, you cannot access the server's endpoint directly.</span></span>

### <a name="manually-serializing-data"></a><span data-ttu-id="ab18e-157">Serializar los datos manualmente</span><span class="sxs-lookup"><span data-stu-id="ab18e-157">Manually serializing data</span></span>

<span data-ttu-id="ab18e-158">SignalR no usarán automáticamente JSON para serializar el método necesidad del allí parámetros de hacerlo manualmente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-158">SignalR will automatically use JSON to serialize your method parameters- there's no need to do it yourself.</span></span>

### <a name="remote-hub-method-not-executed-on-client-in-ondisconnected-function"></a><span data-ttu-id="ab18e-159">Método de concentrador remoto que no se ejecuta en el cliente en función de OnDisconnected</span><span class="sxs-lookup"><span data-stu-id="ab18e-159">Remote Hub method not executed on client in OnDisconnected function</span></span>

<span data-ttu-id="ab18e-160">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="ab18e-160">This behavior is by design.</span></span> <span data-ttu-id="ab18e-161">Cuando `OnDisconnected` es llama, el concentrador ya ha escrito la `Disconnected` estado, que no permite más métodos de concentrador llamarlo.</span><span class="sxs-lookup"><span data-stu-id="ab18e-161">When `OnDisconnected` is called, the hub has already entered the `Disconnected` state, which does not allow further hub methods to be called.</span></span>

<span data-ttu-id="ab18e-162">**Código de servidor de C# que se ejecuta correctamente el código en el evento OnDisconnected**</span><span class="sxs-lookup"><span data-stu-id="ab18e-162">**C# server code that correctly executes code in the OnDisconnected event**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample8.cs)]

### <a name="connection-limit-reached"></a><span data-ttu-id="ab18e-163">Ha alcanzado el límite de conexión</span><span class="sxs-lookup"><span data-stu-id="ab18e-163">Connection limit reached</span></span>

<span data-ttu-id="ab18e-164">Al utilizar la versión completa de IIS en un sistema operativo de cliente como Windows 7, se impone un límite de 10 conexiones.</span><span class="sxs-lookup"><span data-stu-id="ab18e-164">When using the full version of IIS on a client operating system like Windows 7, a 10-connection limit is imposed.</span></span> <span data-ttu-id="ab18e-165">Cuando se usa a un sistema operativo cliente, use IIS Express en su lugar, para evitar este límite.</span><span class="sxs-lookup"><span data-stu-id="ab18e-165">When using a client OS, use IIS Express instead to avoid this limit.</span></span>

### <a name="cross-domain-connection-not-set-up-properly"></a><span data-ttu-id="ab18e-166">Conexión entre dominios no configuró correctamente</span><span class="sxs-lookup"><span data-stu-id="ab18e-166">Cross-domain connection not set up properly</span></span>

<span data-ttu-id="ab18e-167">Si una conexión entre dominios (una conexión para el que no está en el mismo dominio que la página donde se hospeda la dirección URL de SignalR) no está configurada correctamente, la conexión puede producir un error sin mostrar un mensaje de error.</span><span class="sxs-lookup"><span data-stu-id="ab18e-167">If a cross-domain connection (a connection for which the SignalR URL is not in the same domain as the hosting page) is not set up correctly, the connection may fail without an error message.</span></span> <span data-ttu-id="ab18e-168">Para obtener información acerca de cómo habilitar la comunicación entre dominios, consulte [cómo establecer una conexión entre dominios](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span><span class="sxs-lookup"><span data-stu-id="ab18e-168">For information on how to enable cross-domain communication, see [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span></span>

### <a name="connection-using-ntlm-active-directory-not-working-in-net-client"></a><span data-ttu-id="ab18e-169">Conexión mediante NTLM (Active Directory) no funciona en el cliente de .NET</span><span class="sxs-lookup"><span data-stu-id="ab18e-169">Connection using NTLM (Active Directory) not working in .NET client</span></span>

<span data-ttu-id="ab18e-170">Una conexión en una aplicación de cliente de .NET que utiliza la seguridad de dominio puede producir un error si la conexión no está configurada correctamente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-170">A connection in a .NET client application that uses Domain security may fail if the connection is not configured properly.</span></span> <span data-ttu-id="ab18e-171">Para utilizar SignalR en un entorno de dominio, establezca la propiedad de conexión necesaria como se indica a continuación:</span><span class="sxs-lookup"><span data-stu-id="ab18e-171">To use SignalR in a domain environment, set the requisite connection property as follows:</span></span>

<span data-ttu-id="ab18e-172">**Código de cliente de C# que implementa las credenciales de conexión**</span><span class="sxs-lookup"><span data-stu-id="ab18e-172">**C# client code that implements connection credentials**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample9.cs)]

<a id="other"></a>

## <a name="other-connection-issues"></a><span data-ttu-id="ab18e-173">Otros problemas de conexión</span><span class="sxs-lookup"><span data-stu-id="ab18e-173">Other connection issues</span></span>

<span data-ttu-id="ab18e-174">Esta sección describe las causas y soluciones para los síntomas específicos o mensajes de error que se producen durante una conexión.</span><span class="sxs-lookup"><span data-stu-id="ab18e-174">This section describes the causes and solutions for specific symptoms or error messages that occur during a connection.</span></span>

### <a name="start-must-be-called-before-data-can-be-sent-error"></a><span data-ttu-id="ab18e-175">Error de "Inicio se debe llamar antes de que se pueden enviar datos"</span><span class="sxs-lookup"><span data-stu-id="ab18e-175">"Start must be called before data can be sent" error</span></span>

<span data-ttu-id="ab18e-176">Este error se suele producir si el código hace referencia a objetos de SignalR antes de inicia la conexión.</span><span class="sxs-lookup"><span data-stu-id="ab18e-176">This error is commonly seen if code references SignalR objects before the connection is started.</span></span> <span data-ttu-id="ab18e-177">La conexión para los controladores y similares que se llaman a los métodos definidos en el servidor debe agregarse una vez completada la conexión.</span><span class="sxs-lookup"><span data-stu-id="ab18e-177">The wireup for handlers and the like that will call methods defined on the server must be added after the connection completes.</span></span> <span data-ttu-id="ab18e-178">Tenga en cuenta que la llamada a `Start` es asincrónica, por lo que el código después de la llamada se puede ejecutar antes de que se completa.</span><span class="sxs-lookup"><span data-stu-id="ab18e-178">Note that the call to `Start` is asynchronous, so code after the call may be executed before it completes.</span></span> <span data-ttu-id="ab18e-179">La mejor manera de agregar controladores después de iniciar una conexión completamente es colocarlos en una función de devolución de llamada que se pasa como parámetro al método start:</span><span class="sxs-lookup"><span data-stu-id="ab18e-179">The best way to add handlers after a connection starts completely is to put them into a callback function that is passed as a parameter to the start method:</span></span>

<span data-ttu-id="ab18e-180">**Código de cliente de JavaScript que se agrega correctamente los controladores de eventos que hacen referencia a objetos de SignalR**</span><span class="sxs-lookup"><span data-stu-id="ab18e-180">**JavaScript client code that correctly adds event handlers that reference SignalR objects**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample10.js?highlight=1)]

<span data-ttu-id="ab18e-181">Este error también se verán si una conexión se detiene mientras todavía se hace referencia objetos de SignalR.</span><span class="sxs-lookup"><span data-stu-id="ab18e-181">This error will also be seen if a connection stops while SignalR objects are still being referenced.</span></span>

### <a name="301-moved-permanently-or-302-moved-temporarily-error"></a><span data-ttu-id="ab18e-182">Error "302 movido temporalmente" o "301 mueve de forma permanente"</span><span class="sxs-lookup"><span data-stu-id="ab18e-182">"301 Moved Permanently" or "302 Moved Temporarily" error</span></span>

<span data-ttu-id="ab18e-183">Este error puede aparecer si el proyecto contiene una carpeta denominada SignalR, que se va a interferir con el proxy crea automáticamente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-183">This error may be seen if the project contains a folder called SignalR, which will interfere with the automatically-created proxy.</span></span> <span data-ttu-id="ab18e-184">Para evitar este error, no use una carpeta denominada `SignalR` en sus aplicaciones o la generación de automática de proxy turn off.</span><span class="sxs-lookup"><span data-stu-id="ab18e-184">To avoid this error, do not use a folder called `SignalR` in your application, or turn automatic proxy generation off.</span></span> <span data-ttu-id="ab18e-185">Vea [el Proxy generado y lo que hace que](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy) para obtener más detalles.</span><span class="sxs-lookup"><span data-stu-id="ab18e-185">See [The Generated Proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy) for more details.</span></span>

### <a name="403-forbidden-error-in-net-or-silverlight-client"></a><span data-ttu-id="ab18e-186">Error "403 Prohibido" en el cliente de .NET o Silverlight</span><span class="sxs-lookup"><span data-stu-id="ab18e-186">"403 Forbidden" error in .NET or Silverlight client</span></span>

<span data-ttu-id="ab18e-187">Este error puede producirse en entornos entre dominios y la comunicación entre dominios no está habilitada correctamente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-187">This error may occur in cross-domain environments where cross-domain communication is not properly enabled.</span></span> <span data-ttu-id="ab18e-188">Para obtener información acerca de cómo habilitar la comunicación entre dominios, consulte [cómo establecer una conexión entre dominios](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span><span class="sxs-lookup"><span data-stu-id="ab18e-188">For information on how to enable cross-domain communication, see [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span></span> <span data-ttu-id="ab18e-189">Para establecer una conexión entre dominios en un cliente de Silverlight, vea [las conexiones entre dominios desde clientes Silverlight](../guide-to-the-api/hubs-api-guide-net-client.md#slcrossdomain).</span><span class="sxs-lookup"><span data-stu-id="ab18e-189">To establish a cross-domain connection in a Silverlight client, see [Cross-domain connections from Silverlight clients](../guide-to-the-api/hubs-api-guide-net-client.md#slcrossdomain).</span></span>

### <a name="404-not-found-error"></a><span data-ttu-id="ab18e-190">Error "404 no encontrado"</span><span class="sxs-lookup"><span data-stu-id="ab18e-190">"404 Not Found" error</span></span>

<span data-ttu-id="ab18e-191">Hay varias causas para este problema.</span><span class="sxs-lookup"><span data-stu-id="ab18e-191">There are several causes for this issue.</span></span> <span data-ttu-id="ab18e-192">Compruebe que todos los elementos siguientes:</span><span class="sxs-lookup"><span data-stu-id="ab18e-192">Verify all of the following:</span></span>

- <span data-ttu-id="ab18e-193">**Referencia de dirección de proxy de concentrador no tiene el formato correcto:** este error se suele producir si la referencia a la dirección de proxy de concentrador generado no tiene el formato correcto.</span><span class="sxs-lookup"><span data-stu-id="ab18e-193">**Hub proxy address reference not formatted correctly:** This error is commonly seen if the reference to the generated hub proxy address is not formatted correctly.</span></span> <span data-ttu-id="ab18e-194">Compruebe que la referencia a la dirección de la base de datos central se ha creado correctamente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-194">Verify that the reference to the hub address is made properly.</span></span> <span data-ttu-id="ab18e-195">Vea [cómo hacen referencia al proxy generado dinámicamente](../guide-to-the-api/hubs-api-guide-javascript-client.md#dynamicproxy) para obtener más información.</span><span class="sxs-lookup"><span data-stu-id="ab18e-195">See [How to reference the dynamically generated proxy](../guide-to-the-api/hubs-api-guide-javascript-client.md#dynamicproxy) for details.</span></span>
- <span data-ttu-id="ab18e-196">**Agregar rutas a la aplicación antes de agregar la ruta de concentrador:** si la aplicación utiliza otras rutas, compruebe que la primera ruta agrega es la llamada a `MapHubs`.</span><span class="sxs-lookup"><span data-stu-id="ab18e-196">**Adding routes to application before adding the hub route:** If your application uses other routes, verify that the first route added is the call to `MapHubs`.</span></span>

### <a name="500-internal-server-error"></a><span data-ttu-id="ab18e-197">"500 Internal Server Error"</span><span class="sxs-lookup"><span data-stu-id="ab18e-197">"500 Internal Server Error"</span></span>

<span data-ttu-id="ab18e-198">Se trata de un error muy genérico que podría tener una gran variedad de causas.</span><span class="sxs-lookup"><span data-stu-id="ab18e-198">This is a very generic error that could have a wide variety of causes.</span></span> <span data-ttu-id="ab18e-199">Los detalles del error deben aparecer en el registro de eventos del servidor, o pueden encontrarse a través de depurar el servidor.</span><span class="sxs-lookup"><span data-stu-id="ab18e-199">The details of the error should appear in the server's event log, or can be found through debugging the server.</span></span> <span data-ttu-id="ab18e-200">Puede obtenerse información más detallada del error al activar errores detallados en el servidor.</span><span class="sxs-lookup"><span data-stu-id="ab18e-200">More detailed error information may be obtained by turning on detailed errors on the server.</span></span> <span data-ttu-id="ab18e-201">Para obtener más información, consulte [cómo controlar los errores en la clase de base de datos central](../guide-to-the-api/hubs-api-guide-server.md#handleErrors).</span><span class="sxs-lookup"><span data-stu-id="ab18e-201">For more information, see [How to handle errors in the Hub class](../guide-to-the-api/hubs-api-guide-server.md#handleErrors).</span></span>

### <a name="typeerror-lthubtypegt-is-undefined-error"></a><span data-ttu-id="ab18e-202">"TypeError: &lt;hubType&gt; no está definido" error</span><span class="sxs-lookup"><span data-stu-id="ab18e-202">"TypeError: &lt;hubType&gt; is undefined" error</span></span>

<span data-ttu-id="ab18e-203">Este error se producirá si la llamada a `MapHubs` no se realiza correctamente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-203">This error will result if the call to `MapHubs` is not made properly.</span></span> <span data-ttu-id="ab18e-204">Vea [cómo registrar la ruta de SignalR y configurar las opciones de SignalR](../guide-to-the-api/hubs-api-guide-server.md#route) para obtener más información.</span><span class="sxs-lookup"><span data-stu-id="ab18e-204">See [How to register the SignalR route and configure SignalR options](../guide-to-the-api/hubs-api-guide-server.md#route) for more information.</span></span>

### <a name="jsonserializationexception-was-unhandled-by-user-code"></a><span data-ttu-id="ab18e-205">JsonSerializationException no estaba controlada por código de usuario</span><span class="sxs-lookup"><span data-stu-id="ab18e-205">JsonSerializationException was unhandled by user code</span></span>

<span data-ttu-id="ab18e-206">Compruebe que los parámetros que se envía a los métodos no incluyen tipos no son serializables (por ejemplo, identificadores de archivo o las conexiones de base de datos).</span><span class="sxs-lookup"><span data-stu-id="ab18e-206">Verify that the parameters you send to your methods do not include non-serializable types (like file handles or database connections).</span></span> <span data-ttu-id="ab18e-207">Si necesita usar miembros en un objeto de servidor que no desea que se envía al cliente (ya sea por motivos de serialización o de seguridad), use el `JSONIgnore` atributo.</span><span class="sxs-lookup"><span data-stu-id="ab18e-207">If you need to use members on a server-side object that you don't want to be sent to the client (either for security or for reasons of serialization), use the `JSONIgnore` attribute.</span></span>

### <a name="protocol-error-unknown-transport-error"></a><span data-ttu-id="ab18e-208">"Error de protocolo: transporte desconocido" error</span><span class="sxs-lookup"><span data-stu-id="ab18e-208">"Protocol error: Unknown transport" error</span></span>

<span data-ttu-id="ab18e-209">Este error puede producirse si el cliente no es compatible con los transportes que usa SignalR.</span><span class="sxs-lookup"><span data-stu-id="ab18e-209">This error may occur if the client does not support the transports that SignalR uses.</span></span> <span data-ttu-id="ab18e-210">Vea [transportes y reservas](../getting-started/introduction-to-signalr.md#transports) para obtener información en el que se pueden utilizar los exploradores con SignalR.</span><span class="sxs-lookup"><span data-stu-id="ab18e-210">See [Transports and Fallbacks](../getting-started/introduction-to-signalr.md#transports) for information on which browsers can be used with SignalR.</span></span>

### <a name="javascript-hub-proxy-generation-has-been-disabled"></a><span data-ttu-id="ab18e-211">"Se ha deshabilitado generación de proxy del JavaScript Hub."</span><span class="sxs-lookup"><span data-stu-id="ab18e-211">"JavaScript Hub proxy generation has been disabled."</span></span>

<span data-ttu-id="ab18e-212">Este error se producirá si `DisableJavaScriptProxies` mientras también incluye una referencia al proxy generado dinámicamente en `signalr/hubs`.</span><span class="sxs-lookup"><span data-stu-id="ab18e-212">This error will occur if `DisableJavaScriptProxies` is set while also including a reference to the dynamically generated proxy at `signalr/hubs`.</span></span> <span data-ttu-id="ab18e-213">Para obtener más información acerca de cómo crear el proxy manualmente, consulte [el proxy generado y lo que hace que](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span><span class="sxs-lookup"><span data-stu-id="ab18e-213">For more information on creating the proxy manually, see [The generated proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span></span>

### <a name="the-connection-id-is-in-the-incorrect-format-or-the-user-identity-cannot-change-during-an-active-signalr-connection-error"></a><span data-ttu-id="ab18e-214">"El identificador de conexión está en un formato incorrecto" o "no se puede cambiar la identidad del usuario durante una conexión de SignalR activa" error</span><span class="sxs-lookup"><span data-stu-id="ab18e-214">"The connection ID is in the incorrect format" or "The user identity cannot change during an active SignalR connection" error</span></span>

<span data-ttu-id="ab18e-215">Este error puede aparecer si se utiliza la autenticación y el cliente se cerró la sesión antes de que la conexión se ha detenido.</span><span class="sxs-lookup"><span data-stu-id="ab18e-215">This error may be seen if authentication is being used, and the client is logged out before the connection is stopped.</span></span> <span data-ttu-id="ab18e-216">La solución consiste en detener la conexión de SignalR antes de cerrar la sesión del cliente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-216">The solution is to stop the SignalR connection before logging the client out.</span></span>

### <a name="uncaught-error-signalr-jquery-not-found-please-ensure-jquery-is-referenced-before-the-signalrjs-file-error"></a><span data-ttu-id="ab18e-217">"No detectada Error: SignalR: no se encontró de jQuery.</span><span class="sxs-lookup"><span data-stu-id="ab18e-217">"Uncaught Error: SignalR: jQuery not found.</span></span> <span data-ttu-id="ab18e-218">Asegúrese de que se hace referencia a jQuery antes del archivo SignalR.js"error</span><span class="sxs-lookup"><span data-stu-id="ab18e-218">Please ensure jQuery is referenced before the SignalR.js file" error</span></span>

<span data-ttu-id="ab18e-219">El cliente de SignalR JavaScript requiere jQuery para ejecutar.</span><span class="sxs-lookup"><span data-stu-id="ab18e-219">The SignalR JavaScript client requires jQuery to run.</span></span> <span data-ttu-id="ab18e-220">Compruebe que la referencia a jQuery es correcta, que usa la ruta de acceso es válida y que la referencia a jQuery es antes de la referencia de SignalR.</span><span class="sxs-lookup"><span data-stu-id="ab18e-220">Verify that your reference to jQuery is correct, that the path used is valid, and that the reference to jQuery is before the reference to SignalR.</span></span>

### <a name="uncaught-typeerror-cannot-read-property-ltpropertygt-of-undefined-error"></a><span data-ttu-id="ab18e-221">"No detectada TypeError: no se puede leer la propiedad '&lt;propiedad&gt;" undefined "error</span><span class="sxs-lookup"><span data-stu-id="ab18e-221">"Uncaught TypeError: Cannot read property '&lt;property&gt;' of undefined" error</span></span>

<span data-ttu-id="ab18e-222">Este error resultante de no tener jQuery o el proxy de concentradores al que hace referencia correctamente.</span><span class="sxs-lookup"><span data-stu-id="ab18e-222">This error results from not having jQuery or the hubs proxy referenced properly.</span></span> <span data-ttu-id="ab18e-223">Compruebe que la referencia a jQuery y el proxy de concentradores es correcta, que usa la ruta de acceso es válida y que es la referencia a jQuery antes que la referencia al proxy de concentradores.</span><span class="sxs-lookup"><span data-stu-id="ab18e-223">Verify that your reference to jQuery and the hubs proxy is correct, that the path used is valid, and that the reference to jQuery is before the reference to the hubs proxy.</span></span> <span data-ttu-id="ab18e-224">La referencia predeterminada para el proxy de concentradores debe ser similar al siguiente:</span><span class="sxs-lookup"><span data-stu-id="ab18e-224">The default reference to the hubs proxy should look like the following:</span></span>

<span data-ttu-id="ab18e-225">**Código de cliente HTML que hace referencia correctamente al proxy de concentradores**</span><span class="sxs-lookup"><span data-stu-id="ab18e-225">**HTML client-side code that correctly references the Hubs proxy**</span></span>

[!code-html[Main](troubleshooting/samples/sample11.html)]

### <a name="runtimebinderexception-was-unhandled-by-user-code-error"></a><span data-ttu-id="ab18e-226">Error "RuntimeBinderException no estaba controlada por código de usuario"</span><span class="sxs-lookup"><span data-stu-id="ab18e-226">"RuntimeBinderException was unhandled by user code" error</span></span>

<span data-ttu-id="ab18e-227">Este error puede producirse cuando la sobrecarga incorrecta de `Hub.On` se utiliza.</span><span class="sxs-lookup"><span data-stu-id="ab18e-227">This error may occur when the incorrect overload of `Hub.On` is used.</span></span> <span data-ttu-id="ab18e-228">Si el método tiene un valor devuelto, el tipo de valor devuelto debe especificarse como un parámetro de tipo genérico:</span><span class="sxs-lookup"><span data-stu-id="ab18e-228">If the method has a return value, the return type must be specified as a generic type parameter:</span></span>

<span data-ttu-id="ab18e-229">**Método definido en el cliente (sin proxy generado)**</span><span class="sxs-lookup"><span data-stu-id="ab18e-229">**Method defined on the client (without generated proxy)**</span></span>

[!code-html[Main](troubleshooting/samples/sample12.html?highlight=1)]

### <a name="connection-id-is-inconsistent-or-connection-breaks-between-page-loads"></a><span data-ttu-id="ab18e-230">Identificador de conexión no es coherente o saltos de conexión entre la carga de página</span><span class="sxs-lookup"><span data-stu-id="ab18e-230">Connection ID is inconsistent or connection breaks between page loads</span></span>

<span data-ttu-id="ab18e-231">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="ab18e-231">This behavior is by design.</span></span> <span data-ttu-id="ab18e-232">Puesto que el objeto de base de datos central se hospeda en el objeto de página, el concentrador se destruye cuando actualice la página.</span><span class="sxs-lookup"><span data-stu-id="ab18e-232">Since the hub object is hosted in the page object, the hub is destroyed when the page refreshes.</span></span> <span data-ttu-id="ab18e-233">Se necesita una aplicación de varias página mantener la asociación entre los usuarios y los identificadores de conexión para que estén coherentes entre las cargas de página.</span><span class="sxs-lookup"><span data-stu-id="ab18e-233">A multi-page application needs to maintain the association between users and connection IDs so that they will be consistent between page loads.</span></span> <span data-ttu-id="ab18e-234">La identificadores de conexión puede almacenarse en el servidor en la vista una `ConcurrentDictionary` objeto o una base de datos.</span><span class="sxs-lookup"><span data-stu-id="ab18e-234">The connection IDs can be stored on the server in either a `ConcurrentDictionary` object or a database.</span></span>

### <a name="value-cannot-be-null-error"></a><span data-ttu-id="ab18e-235">Error "El valor no puede ser nulo"</span><span class="sxs-lookup"><span data-stu-id="ab18e-235">"Value cannot be null" error</span></span>

<span data-ttu-id="ab18e-236">Métodos de servidor con los parámetros opcionales no se admiten actualmente; Si se omite el parámetro opcional, se producirá un error en el método.</span><span class="sxs-lookup"><span data-stu-id="ab18e-236">Server-side methods with optional parameters are not currently supported; if the optional parameter is omitted, the method will fail.</span></span> <span data-ttu-id="ab18e-237">Para obtener más información, consulte [parámetros opcionales](https://github.com/SignalR/SignalR/issues/324).</span><span class="sxs-lookup"><span data-stu-id="ab18e-237">For more information, see [Optional Parameters](https://github.com/SignalR/SignalR/issues/324).</span></span>

### <a name="firefox-cant-establish-a-connection-to-the-server-at-ltaddressgt-error-in-firebug"></a><span data-ttu-id="ab18e-238">"Firefox no puede establecer una conexión al servidor a &lt;dirección&gt;" error en Firebug</span><span class="sxs-lookup"><span data-stu-id="ab18e-238">"Firefox can't establish a connection to the server at &lt;address&gt;" error in Firebug</span></span>

<span data-ttu-id="ab18e-239">Este mensaje de error puede verse en Firebug si se produce un error en la negociación del transporte de WebSocket y otro transporte se utiliza en su lugar.</span><span class="sxs-lookup"><span data-stu-id="ab18e-239">This error message can be seen in Firebug if negotiation of the WebSocket transport fails and another transport is used instead.</span></span> <span data-ttu-id="ab18e-240">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="ab18e-240">This behavior is by design.</span></span>

### <a name="the-remote-certificate-is-invalid-according-to-the-validation-procedure-error-in-net-client-application"></a><span data-ttu-id="ab18e-241">Error "el certificado remoto no es válido según el procedimiento de validación" en la aplicación cliente de .NET</span><span class="sxs-lookup"><span data-stu-id="ab18e-241">"The remote certificate is invalid according to the validation procedure" error in .NET client application</span></span>

<span data-ttu-id="ab18e-242">Si el servidor requiere certificados de cliente personalizada, a continuación, puede agregar un x509certificate a la conexión antes de que se realiza la solicitud.</span><span class="sxs-lookup"><span data-stu-id="ab18e-242">If your server requires custom client certificates, then you can add an x509certificate to the connection before the request is made.</span></span> <span data-ttu-id="ab18e-243">Agregar el certificado para la conexión con `Connection.AddClientCertificate`.</span><span class="sxs-lookup"><span data-stu-id="ab18e-243">Add the certificate to the connection using `Connection.AddClientCertificate`.</span></span>

### <a name="connection-drops-after-authentication-times-out"></a><span data-ttu-id="ab18e-244">Fallo de conexión una vez agota el tiempo de espera de autenticación</span><span class="sxs-lookup"><span data-stu-id="ab18e-244">Connection drops after authentication times out</span></span>

<span data-ttu-id="ab18e-245">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="ab18e-245">This behavior is by design.</span></span> <span data-ttu-id="ab18e-246">Las credenciales de autenticación no se puede modificar mientras una conexión está activa; Para actualizar las credenciales, debe detener y reiniciar la conexión.</span><span class="sxs-lookup"><span data-stu-id="ab18e-246">Authentication credentials cannot be modified while a connection is active; to refresh credentials, the connection must be stopped and restarted.</span></span>

### <a name="onconnected-gets-called-twice-when-using-jquery-mobile"></a><span data-ttu-id="ab18e-247">OnConnected se invoca dos veces al uso de jQuery Mobile</span><span class="sxs-lookup"><span data-stu-id="ab18e-247">OnConnected gets called twice when using jQuery Mobile</span></span>

<span data-ttu-id="ab18e-248">jQuery Mobile `initializePage` función fuerza las secuencias de comandos en cada página para volver a ejecutar, creando así una segunda conexión.</span><span class="sxs-lookup"><span data-stu-id="ab18e-248">jQuery Mobile's `initializePage` function forces the scripts in each page to be re-executed, thus creating a second connection.</span></span> <span data-ttu-id="ab18e-249">Soluciones para este problema incluyen:</span><span class="sxs-lookup"><span data-stu-id="ab18e-249">Solutions for this issue include:</span></span>

- <span data-ttu-id="ab18e-250">Incluir la referencia a jQuery Mobile antes de que el archivo de JavaScript.</span><span class="sxs-lookup"><span data-stu-id="ab18e-250">Include the reference to jQuery Mobile before your JavaScript file.</span></span>
- <span data-ttu-id="ab18e-251">Deshabilitar la `initializePage` función estableciendo `$.mobile.autoInitializePage = false`.</span><span class="sxs-lookup"><span data-stu-id="ab18e-251">Disable the `initializePage` function by setting `$.mobile.autoInitializePage = false`.</span></span>
- <span data-ttu-id="ab18e-252">Espere a que la página para finalizar la inicialización antes de iniciar la conexión.</span><span class="sxs-lookup"><span data-stu-id="ab18e-252">Wait for the page to finish initializing before starting the connection.</span></span>

### <a name="messages-are-delayed-in-silverlight-applications-using-server-sent-events"></a><span data-ttu-id="ab18e-253">Los mensajes están retrasados en aplicaciones de Silverlight con eventos enviados del servidor</span><span class="sxs-lookup"><span data-stu-id="ab18e-253">Messages are delayed in Silverlight applications using Server Sent Events</span></span>

<span data-ttu-id="ab18e-254">Los mensajes están retrasados cuando usa servidor envía eventos de Silverlight.</span><span class="sxs-lookup"><span data-stu-id="ab18e-254">Messages are delayed when using server sent events on Silverlight.</span></span> <span data-ttu-id="ab18e-255">Para forzar el tiempo de sondeo para usarse en su lugar, utilice lo siguiente al iniciar la conexión:</span><span class="sxs-lookup"><span data-stu-id="ab18e-255">To force long polling to be used instead, use the following when starting the connection:</span></span>

[!code-css[Main](troubleshooting/samples/sample13.css)]

### <a name="permission-denied-using-forever-frame-protocol"></a><span data-ttu-id="ab18e-256">Uso de "Permiso denegado" Enmarcar indefinidamente protocolo</span><span class="sxs-lookup"><span data-stu-id="ab18e-256">"Permission Denied" using Forever Frame protocol</span></span>

<span data-ttu-id="ab18e-257">Se trata de un problema conocido, se describe [aquí](https://github.com/SignalR/SignalR/issues/1963).</span><span class="sxs-lookup"><span data-stu-id="ab18e-257">This is a known issue, described [here](https://github.com/SignalR/SignalR/issues/1963).</span></span> <span data-ttu-id="ab18e-258">Este síntoma puede verse mediante la biblioteca de JQuery más reciente; la solución consiste en cambiar la aplicación para JQuery 1.8.2.</span><span class="sxs-lookup"><span data-stu-id="ab18e-258">This symptom may be seen using the latest JQuery library; the workaround is to downgrade your application to JQuery 1.8.2.</span></span>

<a id="server"></a>

## <a name="compilation-and-server-side-errors"></a><span data-ttu-id="ab18e-259">Errores de compilación y el lado servidor</span><span class="sxs-lookup"><span data-stu-id="ab18e-259">Compilation and server-side errors</span></span>

 <span data-ttu-id="ab18e-260">La sección siguiente contiene posibles soluciones al compilador y errores en tiempo de ejecución del servidor.</span><span class="sxs-lookup"><span data-stu-id="ab18e-260">The following section contains possible solutions to compiler and server-side runtime errors.</span></span> 

### <a name="reference-to-hub-instance-is-null"></a><span data-ttu-id="ab18e-261">Referencia a la instancia de base de datos central es null</span><span class="sxs-lookup"><span data-stu-id="ab18e-261">Reference to Hub instance is null</span></span>

<span data-ttu-id="ab18e-262">Puesto que se crea una instancia de base de datos central para cada conexión, no se puede crear una instancia de un concentrador en el código usted mismo.</span><span class="sxs-lookup"><span data-stu-id="ab18e-262">Since a hub instance is created for each connection, you can't create an instance of a hub in your code yourself.</span></span> <span data-ttu-id="ab18e-263">Para llamar a métodos en un centro de fuera del concentrador propio, vea [cómo llamar a los métodos de cliente y administrar grupos desde fuera de la clase de base de datos central](../guide-to-the-api/hubs-api-guide-server.md#callfromoutsidehub) para saber cómo obtener una referencia para el contexto del concentrador.</span><span class="sxs-lookup"><span data-stu-id="ab18e-263">To call methods on a hub from outside the hub itself, see [How to call client methods and manage groups from outside the Hub class](../guide-to-the-api/hubs-api-guide-server.md#callfromoutsidehub) for how to obtain a reference to the hub context.</span></span>

### <a name="httpcontextcurrentsession-is-null"></a><span data-ttu-id="ab18e-264">HTTPContext.Current.Session es null</span><span class="sxs-lookup"><span data-stu-id="ab18e-264">HTTPContext.Current.Session is null</span></span>

<span data-ttu-id="ab18e-265">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="ab18e-265">This behavior is by design.</span></span> <span data-ttu-id="ab18e-266">SignalR no es compatible con el estado de sesión ASP.NET, puesto que habilitar el estado de sesión, se interrumpiría mensajería dúplex.</span><span class="sxs-lookup"><span data-stu-id="ab18e-266">SignalR does not support the ASP.NET session state, since enabling the session state would break duplex messaging.</span></span>

### <a name="no-suitable-method-to-override"></a><span data-ttu-id="ab18e-267">Ningún método adecuado para invalidar</span><span class="sxs-lookup"><span data-stu-id="ab18e-267">No suitable method to override</span></span>

<span data-ttu-id="ab18e-268">Puede ver este error si se utiliza el código de la documentación anterior o blogs.</span><span class="sxs-lookup"><span data-stu-id="ab18e-268">You may see this error if you are using code from older documentation or blogs.</span></span> <span data-ttu-id="ab18e-269">Compruebe que no se hace referencia a nombres de métodos que se han cambiado o en desuso (como `OnConnectedAsync`).</span><span class="sxs-lookup"><span data-stu-id="ab18e-269">Verify that you are not referencing names of methods that have been changed or deprecated (like `OnConnectedAsync`).</span></span>

### <a name="hostcontextextensionswebsocketserverurl-is-null"></a><span data-ttu-id="ab18e-270">HostContextExtensions.WebSocketServerUrl es nulo</span><span class="sxs-lookup"><span data-stu-id="ab18e-270">HostContextExtensions.WebSocketServerUrl is null</span></span>

<span data-ttu-id="ab18e-271">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="ab18e-271">This behavior is by design.</span></span> <span data-ttu-id="ab18e-272">Este miembro está en desuso y no debe usarse.</span><span class="sxs-lookup"><span data-stu-id="ab18e-272">This member is deprecated and should not be used.</span></span>

### <a name="a-route-named-signalrhubs-is-already-in-the-route-collection-error"></a><span data-ttu-id="ab18e-273">Error "una ruta denominada 'signalr.hubs' ya está en la colección de rutas"</span><span class="sxs-lookup"><span data-stu-id="ab18e-273">"A route named 'signalr.hubs' is already in the route collection" error</span></span>

<span data-ttu-id="ab18e-274">Verá este error si `MapHubs` se llama dos veces a la aplicación.</span><span class="sxs-lookup"><span data-stu-id="ab18e-274">This error will be seen if `MapHubs` is called twice by your application.</span></span> <span data-ttu-id="ab18e-275">Algunas aplicaciones de ejemplo llamada `MapHubs` directamente en el archivo de aplicación global; otros usuarios realicen la llamada en una clase contenedora.</span><span class="sxs-lookup"><span data-stu-id="ab18e-275">Some example applications call `MapHubs` directly in the global application file; others make the call in a wrapper class.</span></span> <span data-ttu-id="ab18e-276">Asegúrese de que la aplicación no ambas cosas.</span><span class="sxs-lookup"><span data-stu-id="ab18e-276">Ensure that your application does not do both.</span></span>

<a id="vs"></a>

## <a name="visual-studio-issues"></a><span data-ttu-id="ab18e-277">Problemas de Visual Studio</span><span class="sxs-lookup"><span data-stu-id="ab18e-277">Visual Studio issues</span></span>

<span data-ttu-id="ab18e-278">Esta sección describe problemas que pueden producirse en Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="ab18e-278">This section describes issues encountered in Visual Studio.</span></span>

### <a name="script-documents-node-does-not-appear-in-solution-explorer"></a><span data-ttu-id="ab18e-279">Nodo de documentos de script no aparece en el Explorador de soluciones</span><span class="sxs-lookup"><span data-stu-id="ab18e-279">Script Documents node does not appear in Solution Explorer</span></span>

<span data-ttu-id="ab18e-280">Algunos de nuestros tutoriales le llevan al nodo "Documentos de Script" en el Explorador de soluciones durante la depuración.</span><span class="sxs-lookup"><span data-stu-id="ab18e-280">Some of our tutorials direct you to the "Script Documents" node in Solution Explorer while debugging.</span></span> <span data-ttu-id="ab18e-281">Este nodo se genera mediante el depurador de JavaScript y solo se mostrará durante la depuración de los clientes de explorador en Internet Explorer; el nodo no aparecerá si se utiliza Chrome o Firefox.</span><span class="sxs-lookup"><span data-stu-id="ab18e-281">This node is produced by the JavaScript debugger, and will only appear while debugging browser clients in Internet Explorer; the node will not appear if Chrome or Firefox are used.</span></span> <span data-ttu-id="ab18e-282">El depurador de JavaScript no ejecutará si está ejecutando otro depurador de cliente, por ejemplo, el depurador de Silverlight.</span><span class="sxs-lookup"><span data-stu-id="ab18e-282">The JavaScript debugger will also not run if another client debugger is running, such as the Silverlight debugger.</span></span>

### <a name="signalr-does-not-work-on-visual-studio-2008-or-earlier"></a><span data-ttu-id="ab18e-283">SignalR no funciona en Visual Studio 2008 o versiones anteriores</span><span class="sxs-lookup"><span data-stu-id="ab18e-283">SignalR does not work on Visual Studio 2008 or earlier</span></span>

<span data-ttu-id="ab18e-284">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="ab18e-284">This behavior is by design.</span></span> <span data-ttu-id="ab18e-285">SignalR requiere .NET Framework 4 o posterior; Esto requiere que se desarrollan aplicaciones SignalR en Visual Studio 2010 o posterior.</span><span class="sxs-lookup"><span data-stu-id="ab18e-285">SignalR requires .NET Framework 4 or later; this requires that SignalR applications be developed in Visual Studio 2010 or later.</span></span>

<a id="iis"></a>

## <a name="iis-issues"></a><span data-ttu-id="ab18e-286">Problemas IIS</span><span class="sxs-lookup"><span data-stu-id="ab18e-286">IIS issues</span></span>

<span data-ttu-id="ab18e-287">Esta sección contiene problemas con Internet Information Services.</span><span class="sxs-lookup"><span data-stu-id="ab18e-287">This section contains issues with Internet Information Services.</span></span>

### <a name="web-site-crashes-after-maphubs-call"></a><span data-ttu-id="ab18e-288">Sitio Web se bloquea después de llamar MapHubs</span><span class="sxs-lookup"><span data-stu-id="ab18e-288">Web site crashes after MapHubs call</span></span>

<span data-ttu-id="ab18e-289">Este problema se corrigió en la versión más reciente de SignalR.</span><span class="sxs-lookup"><span data-stu-id="ab18e-289">This issue has been fixed in the latest version of SignalR.</span></span> <span data-ttu-id="ab18e-290">Compruebe que está utilizando la versión publicada más reciente de SignalR mediante la actualización de la instalación mediante NuGet.</span><span class="sxs-lookup"><span data-stu-id="ab18e-290">Verify that you are using the latest released version of SignalR by updating your installation using NuGet.</span></span>

<a id="azure"></a>

## <a name="azure-issues"></a><span data-ttu-id="ab18e-291">Problemas de Azure</span><span class="sxs-lookup"><span data-stu-id="ab18e-291">Azure issues</span></span>

<span data-ttu-id="ab18e-292">Esta sección contiene problemas con Microsoft Azure.</span><span class="sxs-lookup"><span data-stu-id="ab18e-292">This section contains issues with Microsoft Azure.</span></span>

### <a name="messages-are-not-received-through-the-azure-backplane-after-altering-topic-names"></a><span data-ttu-id="ab18e-293">No se reciben los mensajes a través de la placa de Azure después de modificar los nombres de temas</span><span class="sxs-lookup"><span data-stu-id="ab18e-293">Messages are not received through the Azure backplane after altering topic names</span></span>

<span data-ttu-id="ab18e-294">Los temas que se usa el backplane de Azure no pretenden ser configurables por el usuario.</span><span class="sxs-lookup"><span data-stu-id="ab18e-294">The topics used by the Azure backplane are not intended to be user-configurable.</span></span>
