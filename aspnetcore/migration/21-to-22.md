---
title: Migración de ASP.NET Core 2.1 a 2.2
author: scottaddie
description: En este artículo se describe los requisitos previos y pasos más comunes para migrar un proyecto de ASP.NET Core 2.1 a 2.2 de ASP.NET Core.
ms.author: scaddie
ms.custom: mvc
ms.date: 12/11/2018
uid: migration/21-to-22
ms.openlocfilehash: 09602667098592814c0bf41852f64e9a32202b27
ms.sourcegitcommit: b34b25da2ab68e6495b2460ff570468f16a9bf0d
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 12/12/2018
ms.locfileid: "53284557"
---
# <a name="migrate-from-aspnet-core-21-to-22"></a><span data-ttu-id="46cf4-103">Migración de ASP.NET Core 2.1 a 2.2</span><span class="sxs-lookup"><span data-stu-id="46cf4-103">Migrate from ASP.NET Core 2.1 to 2.2</span></span>

<span data-ttu-id="46cf4-104">Por [Scott Addie](https://github.com/scottaddie)</span><span class="sxs-lookup"><span data-stu-id="46cf4-104">By [Scott Addie](https://github.com/scottaddie)</span></span>

<span data-ttu-id="46cf4-105">En este artículo se explica cómo actualizar un proyecto existente de ASP.NET Core 2.1 a 2.2 de ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="46cf4-105">This article explains how to update an existing ASP.NET Core 2.1 project to ASP.NET Core 2.2.</span></span>

[!INCLUDE[](~/includes/net-core-prereqs-all-2.2.md)]

## <a name="update-target-framework-moniker-tfm"></a><span data-ttu-id="46cf4-106">Actualización del moniker de la plataforma de destino (TFM)</span><span class="sxs-lookup"><span data-stu-id="46cf4-106">Update Target Framework Moniker (TFM)</span></span>

<span data-ttu-id="46cf4-107">Los proyectos destinados a .NET Core deben usar el [TFM](/dotnet/standard/frameworks#referring-to-frameworks) de una versión mayor o igual que .NET Core 2.2.</span><span class="sxs-lookup"><span data-stu-id="46cf4-107">Projects targeting .NET Core should use the [TFM](/dotnet/standard/frameworks#referring-to-frameworks) of a version greater than or equal to .NET Core 2.2.</span></span> <span data-ttu-id="46cf4-108">Actualización de la `<TargetFramework>` texto interno del nodo con `netcoreapp2.2`:</span><span class="sxs-lookup"><span data-stu-id="46cf4-108">Update the `<TargetFramework>` node's inner text with `netcoreapp2.2`:</span></span>

```xml
<TargetFramework>netcoreapp2.2</TargetFramework>
```

<span data-ttu-id="46cf4-109">Los proyectos destinados a .NET Framework pueden seguir usando el TFM de una versión mayor o igual que .NET Framework 4.6.1:</span><span class="sxs-lookup"><span data-stu-id="46cf4-109">Projects targeting .NET Framework may continue to use the TFM of a version greater than or equal to .NET Framework 4.6.1:</span></span>

```xml
<TargetFramework>net461</TargetFramework>
```

## <a name="adopt-the-iis-in-process-hosting-model"></a><span data-ttu-id="46cf4-110">Adoptar el modelo de hospedaje de IIS en curso</span><span class="sxs-lookup"><span data-stu-id="46cf4-110">Adopt the IIS in-process hosting model</span></span>

<span data-ttu-id="46cf4-111">Adoptar la [en proceso modelo de hospedaje de IIS](xref:fundamentals/servers/aspnet-core-module#in-process-hosting-model), agregue el `<AspNetCoreHostingModel>` propiedad con un valor de `InProcess` a un `<PropertyGroup>` en el archivo de proyecto:</span><span class="sxs-lookup"><span data-stu-id="46cf4-111">To adopt the [in-process hosting model for IIS](xref:fundamentals/servers/aspnet-core-module#in-process-hosting-model), add the `<AspNetCoreHostingModel>` property with a value of `InProcess` to a `<PropertyGroup>` in the project file:</span></span>

```xml
<AspNetCoreHostingModel>InProcess</AspNetCoreHostingModel>
```

<span data-ttu-id="46cf4-112">Para obtener más información, consulta <xref:host-and-deploy/aspnet-core-module#hosting-model>.</span><span class="sxs-lookup"><span data-stu-id="46cf4-112">For more information, see <xref:host-and-deploy/aspnet-core-module#hosting-model>.</span></span>

## <a name="update-package-references"></a><span data-ttu-id="46cf4-113">Actualización de las referencias del paquete</span><span class="sxs-lookup"><span data-stu-id="46cf4-113">Update package references</span></span>

<span data-ttu-id="46cf4-114">Si el destino es .NET Core, quite el `Version` atributo para la referencia del metapaquete.</span><span class="sxs-lookup"><span data-stu-id="46cf4-114">If targeting .NET Core, remove the `Version` attribute for the metapackage reference.</span></span> <span data-ttu-id="46cf4-115">Inclusión de un `Version` atributo resultados en la siguiente advertencia:</span><span class="sxs-lookup"><span data-stu-id="46cf4-115">Inclusion of a `Version` attribute results in the following warning:</span></span>

```console
A PackageReference to 'Microsoft.AspNetCore.App' specified a Version of `2.2.0`. Specifying the version of this package is not recommended. For more information, see https://aka.ms/sdkimplicitrefs
```

<span data-ttu-id="46cf4-116">La referencia del metapaquete debe parecerse al siguiente `<PackageReference />` nodo:</span><span class="sxs-lookup"><span data-stu-id="46cf4-116">The metapackage reference should resemble the following `<PackageReference />` node:</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore.App" />
</ItemGroup>
```

<span data-ttu-id="46cf4-117">Si el destino es .NET Framework, actualizar cada referencia de paquete `Version` atributo 2.2.0 o posterior.</span><span class="sxs-lookup"><span data-stu-id="46cf4-117">If targeting .NET Framework, update each package reference's `Version` attribute to 2.2.0 or later.</span></span> <span data-ttu-id="46cf4-118">Estas son las referencias del paquete en un proyecto típico de ASP.NET Core 2.2 destinadas a .NET Framework:</span><span class="sxs-lookup"><span data-stu-id="46cf4-118">Here are the package references in a typical ASP.NET Core 2.2 project targeting .NET Framework:</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.CookiePolicy" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.HttpsPolicy" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.Mvc" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.StaticFiles" Version="2.2.0" />
</ItemGroup>
```

## <a name="update-net-core-sdk-version-in-globaljson"></a><span data-ttu-id="46cf4-119">Actualización de la versión del SDK de .NET Core en global.json</span><span class="sxs-lookup"><span data-stu-id="46cf4-119">Update .NET Core SDK version in global.json</span></span>

<span data-ttu-id="46cf4-120">Si la solución se basa en un [global.json](/dotnet/core/tools/global-json) archivo a una versión concreta del SDK de .NET Core, actualice su `version` propiedad a la versión 2.2 instalada en su equipo:</span><span class="sxs-lookup"><span data-stu-id="46cf4-120">If your solution relies upon a [global.json](/dotnet/core/tools/global-json) file to target a specific .NET Core SDK version, update its `version` property to the 2.2 version installed on your machine:</span></span>

```json
{
  "sdk": {
    "version": "2.2.100"
  }
}
```

## <a name="call-configurekestrel-instead-of-usekestrel"></a><span data-ttu-id="46cf4-121">Llamar a ConfigureKestrel en lugar de UseKestrel</span><span class="sxs-lookup"><span data-stu-id="46cf4-121">Call ConfigureKestrel instead of UseKestrel</span></span>

<span data-ttu-id="46cf4-122">Si la aplicación llama a <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> en el `CreateWebHostBuilder` método de la `Program` class, llame a `ConfigureKestrel` en su lugar evitar conflictos con la [IIS modelo de hospedaje en proceso](xref:fundamentals/servers/aspnet-core-module#in-process-hosting-model):</span><span class="sxs-lookup"><span data-stu-id="46cf4-122">If the app calls <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> in the `CreateWebHostBuilder` method of the `Program` class, call `ConfigureKestrel` instead to avoid conflicts with the [IIS in-process hosting model](xref:fundamentals/servers/aspnet-core-module#in-process-hosting-model):</span></span>

```csharp
public static IWebHostBuilder CreateWebHostBuilder(string[] args) =>
    WebHost.CreateDefaultBuilder(args)
        .UseStartup<Startup>()
        .ConfigureKestrel((context, options) =>
        {
            // Set properties and call methods on options
        });
```

<span data-ttu-id="46cf4-123">Para obtener más información, consulta <xref:fundamentals/servers/kestrel#how-to-use-kestrel-in-aspnet-core-apps>.</span><span class="sxs-lookup"><span data-stu-id="46cf4-123">For more information, see <xref:fundamentals/servers/kestrel#how-to-use-kestrel-in-aspnet-core-apps>.</span></span>

## <a name="update-compatibility-version"></a><span data-ttu-id="46cf4-124">Versión de actualización de compatibilidad</span><span class="sxs-lookup"><span data-stu-id="46cf4-124">Update compatibility version</span></span>

<span data-ttu-id="46cf4-125">Actualice la versión de compatibilidad en `Startup.ConfigureServices` a `Version_2_2`:</span><span class="sxs-lookup"><span data-stu-id="46cf4-125">Update the compatibility version in `Startup.ConfigureServices` to `Version_2_2`:</span></span>

```csharp
services.AddMvc()
        .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

## <a name="update-cors-policy"></a><span data-ttu-id="46cf4-126">Actualizar directiva CORS</span><span class="sxs-lookup"><span data-stu-id="46cf4-126">Update CORS policy</span></span>

<span data-ttu-id="46cf4-127">En ASP.NET Core 2.2, el middleware de CORS responde con un origen de carácter comodín (`*`) si una directiva permite que cualquier origen y permite que las credenciales.</span><span class="sxs-lookup"><span data-stu-id="46cf4-127">In ASP.NET Core 2.2, the CORS middleware responds with a wildcard origin (`*`) if a policy allows any origin and allows credentials.</span></span> <span data-ttu-id="46cf4-128">No se admiten las credenciales cuando un origen de carácter comodín (`*`) se especifica, y los exploradores no permitirá la solicitud de CORS.</span><span class="sxs-lookup"><span data-stu-id="46cf4-128">Credentials aren't supported when a wildcard origin (`*`) is specified, and browsers will disallow the CORS request.</span></span> <span data-ttu-id="46cf4-129">Para obtener más información, incluidas las opciones para corregir el problema en el cliente, consulte el [documentos de MDN web](https://developer.mozilla.org/docs/Web/HTTP/CORS/Errors/CORSNotSupportingCredentials).</span><span class="sxs-lookup"><span data-stu-id="46cf4-129">For more information, including options for correcting the problem on the client, see the [MDN web docs](https://developer.mozilla.org/docs/Web/HTTP/CORS/Errors/CORSNotSupportingCredentials).</span></span>

<span data-ttu-id="46cf4-130">Para corregir este problema en el servidor, realice una de las siguientes acciones:</span><span class="sxs-lookup"><span data-stu-id="46cf4-130">To correct this problem on the server, take one of the following actions:</span></span>

* <span data-ttu-id="46cf4-131">Modificar la directiva CORS para no permitir credenciales.</span><span class="sxs-lookup"><span data-stu-id="46cf4-131">Modify the CORS policy to no longer allow credentials.</span></span> <span data-ttu-id="46cf4-132">Es decir, quite la llamada a <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowCredentials*> al configurar la directiva.</span><span class="sxs-lookup"><span data-stu-id="46cf4-132">That is, remove the call to <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowCredentials*> when configuring the policy.</span></span>
* <span data-ttu-id="46cf4-133">Si se necesitan credenciales para la solicitud de CORS se realice correctamente, modifique la directiva para especificar los hosts permitidos.</span><span class="sxs-lookup"><span data-stu-id="46cf4-133">If credentials are required for the CORS request to succeed, modify the policy to specify allowed hosts.</span></span> <span data-ttu-id="46cf4-134">Por ejemplo, usar `builder.WithOrigins("https://api.example1.com", "https://example2.com")` en lugar de usar <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowAnyOrigin*>.</span><span class="sxs-lookup"><span data-stu-id="46cf4-134">For example, use `builder.WithOrigins("https://api.example1.com", "https://example2.com")` instead of using <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowAnyOrigin*>.</span></span>

## <a name="update-docker-images"></a><span data-ttu-id="46cf4-135">Actualizar las imágenes de Docker</span><span class="sxs-lookup"><span data-stu-id="46cf4-135">Update Docker images</span></span>

<span data-ttu-id="46cf4-136">La siguiente tabla muestra los cambios de la etiqueta de la imagen de Docker:</span><span class="sxs-lookup"><span data-stu-id="46cf4-136">The following table shows the Docker image tag changes:</span></span>

| <span data-ttu-id="46cf4-137">2.1</span><span class="sxs-lookup"><span data-stu-id="46cf4-137">2.1</span></span>                                       | <span data-ttu-id="46cf4-138">2.2</span><span class="sxs-lookup"><span data-stu-id="46cf4-138">2.2</span></span>                                       |
| ----------------------------------------- | ----------------------------------------- |
| `microsoft/dotnet:2.1-aspnetcore-runtime` | `microsoft/dotnet:2.2-aspnetcore-runtime` |
| `microsoft/dotnet:2.1-sdk`                | `microsoft/dotnet:2.2-sdk`                |

<span data-ttu-id="46cf4-139">Cambiar el `FROM` líneas en su *Dockerfile* para usar las nuevas etiquetas de imagen en la columna 2.2 de la tabla anterior.</span><span class="sxs-lookup"><span data-stu-id="46cf4-139">Change the `FROM` lines in your *Dockerfile* to use the new image tags in the preceding table's 2.2 column.</span></span>

## <a name="build-manually-in-visual-studio-when-using-iis-in-process-hosting"></a><span data-ttu-id="46cf4-140">Compilar manualmente en Visual Studio cuando se usa en el proceso de hospedaje de IIS</span><span class="sxs-lookup"><span data-stu-id="46cf4-140">Build manually in Visual Studio when using IIS in-process hosting</span></span>

<span data-ttu-id="46cf4-141">Visual Studio **la generación automática de la solicitud del explorador** experiencia no funcionará con la [IIS modelo de hospedaje en proceso](xref:fundamentals/servers/aspnet-core-module#in-process-hosting-model).</span><span class="sxs-lookup"><span data-stu-id="46cf4-141">Visual Studio's **Auto build on browser request** experience doesn't function with the [IIS in-process hosting model](xref:fundamentals/servers/aspnet-core-module#in-process-hosting-model).</span></span> <span data-ttu-id="46cf4-142">Debe recompilar manualmente el proyecto cuando se usa en el proceso de hospedaje.</span><span class="sxs-lookup"><span data-stu-id="46cf4-142">You must manually rebuild the project when using in-process hosting.</span></span> <span data-ttu-id="46cf4-143">Mejoras en esta experiencia se planean para una futura versión de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="46cf4-143">Improvements to this experience are planned for a future release of Visual Studio.</span></span>

## <a name="additional-resources"></a><span data-ttu-id="46cf4-144">Recursos adicionales</span><span class="sxs-lookup"><span data-stu-id="46cf4-144">Additional resources</span></span>

* <xref:mvc/compatibility-version>
* <xref:fundamentals/metapackage-app>
* [<span data-ttu-id="46cf4-145">Referencias implícitas del paquete</span><span class="sxs-lookup"><span data-stu-id="46cf4-145">Implicit package references</span></span>](/dotnet/core/tools/csproj#implicit-package-references)
