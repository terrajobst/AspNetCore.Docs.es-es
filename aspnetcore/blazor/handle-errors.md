---
title: Control de errores en ASP.NET Core aplicaciones increíbles
author: guardrex
description: Descubra cómo ASP.NET Core increíble el grado de administración de las excepciones no controladas y cómo desarrollar aplicaciones que detecten y controlen los errores.
monikerRange: '>= aspnetcore-3.0'
ms.author: riande
ms.custom: mvc
ms.date: 09/23/2019
uid: blazor/handle-errors
ms.openlocfilehash: de0a2f74df84f41581ac93dbeec7a5c5e90c6fa2
ms.sourcegitcommit: 79eeb17604b536e8f34641d1e6b697fb9a2ee21f
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 09/24/2019
ms.locfileid: "71207199"
---
# <a name="handle-errors-in-aspnet-core-blazor-apps"></a><span data-ttu-id="0f6ed-103">Control de errores en ASP.NET Core aplicaciones increíbles</span><span class="sxs-lookup"><span data-stu-id="0f6ed-103">Handle errors in ASP.NET Core Blazor apps</span></span>

<span data-ttu-id="0f6ed-104">Por [Steve Sanderson](https://github.com/SteveSandersonMS)</span><span class="sxs-lookup"><span data-stu-id="0f6ed-104">By [Steve Sanderson](https://github.com/SteveSandersonMS)</span></span>

<span data-ttu-id="0f6ed-105">En este artículo se describe el modo en que el increíble grado de administración de las excepciones no controladas y el modo de desarrollar aplicaciones que detectan y controlan los errores.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-105">This article describes how Blazor manages unhandled exceptions and how to develop apps that detect and handle errors.</span></span>

## <a name="how-the-blazor-framework-reacts-to-unhandled-exceptions"></a><span data-ttu-id="0f6ed-106">Cómo reacciona el marco de increíbles a las excepciones no controladas</span><span class="sxs-lookup"><span data-stu-id="0f6ed-106">How the Blazor framework reacts to unhandled exceptions</span></span>

<span data-ttu-id="0f6ed-107">El servidor es un marco con estado.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-107">Blazor Server is a stateful framework.</span></span> <span data-ttu-id="0f6ed-108">Mientras los usuarios interactúan con una aplicación, mantienen una conexión con el servidor conocido como *circuito*.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-108">While users interact with an app, they maintain a connection to the server known as a *circuit*.</span></span> <span data-ttu-id="0f6ed-109">El circuito contiene instancias de componentes activas, además de muchos otros aspectos del estado, como:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-109">The circuit holds active component instances, plus many other aspects of state, such as:</span></span>

* <span data-ttu-id="0f6ed-110">La salida representada más reciente de los componentes.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-110">The most recent rendered output of components.</span></span>
* <span data-ttu-id="0f6ed-111">Conjunto actual de delegados de control de eventos que pueden ser desencadenados por eventos del cliente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-111">The current set of event-handling delegates that could be triggered by client-side events.</span></span>

<span data-ttu-id="0f6ed-112">Si un usuario abre la aplicación en varias pestañas del explorador, tiene varios circuitos independientes.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-112">If a user opens the app in multiple browser tabs, they have multiple independent circuits.</span></span>

<span data-ttu-id="0f6ed-113">El increíbledor trata las excepciones no controladas como graves para el circuito en el que se producen.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-113">Blazor treats most unhandled exceptions as fatal to the circuit where they occur.</span></span> <span data-ttu-id="0f6ed-114">Si se termina un circuito debido a una excepción no controlada, el usuario solo puede seguir interactuando con la aplicación recargando la página para crear un nuevo circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-114">If a circuit is terminated due to an unhandled exception, the user can only continue to interact with the app by reloading the page to create a new circuit.</span></span> <span data-ttu-id="0f6ed-115">Los circuitos fuera de la terminación, que son circuitos para otros usuarios u otras pestañas del explorador, no se ven afectados.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-115">Circuits outside of the one that's terminated, which are circuits for other users or other browser tabs, aren't affected.</span></span> <span data-ttu-id="0f6ed-116">Este escenario es similar a una aplicación de escritorio que bloquea&mdash;la aplicación bloqueada debe reiniciarse, pero otras aplicaciones no se ven afectadas.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-116">This scenario is similar to a desktop app that crashes&mdash;the crashed app must be restarted, but other apps aren't affected.</span></span>

<span data-ttu-id="0f6ed-117">Un circuito finaliza cuando se produce una excepción no controlada por los siguientes motivos:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-117">A circuit is terminated when an unhandled exception occurs for the following reasons:</span></span>

* <span data-ttu-id="0f6ed-118">Una excepción no controlada a menudo deja el circuito en un estado indefinido.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-118">An unhandled exception often leaves the circuit in an undefined state.</span></span>
* <span data-ttu-id="0f6ed-119">No se puede garantizar la operación normal de la aplicación después de una excepción no controlada.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-119">The app's normal operation can't be guaranteed after an unhandled exception.</span></span>
* <span data-ttu-id="0f6ed-120">Es posible que aparezcan vulnerabilidades de seguridad en la aplicación si el circuito continúa.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-120">Security vulnerabilities may appear in the app if the circuit continues.</span></span>

## <a name="manage-unhandled-exceptions-in-developer-code"></a><span data-ttu-id="0f6ed-121">Administrar excepciones no controladas en el código de desarrollador</span><span class="sxs-lookup"><span data-stu-id="0f6ed-121">Manage unhandled exceptions in developer code</span></span>

<span data-ttu-id="0f6ed-122">Para que una aplicación continúe después de un error, la aplicación debe tener una lógica de control de errores.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-122">For an app to continue after an error, the app must have error handling logic.</span></span> <span data-ttu-id="0f6ed-123">En secciones posteriores de este artículo se describen posibles orígenes de excepciones no controladas.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-123">Later sections of this article describe potential sources of unhandled exceptions.</span></span>

<span data-ttu-id="0f6ed-124">En producción, no se representan mensajes de excepción de marco o seguimientos de pila en la interfaz de usuario.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-124">In production, don't render framework exception messages or stack traces in the UI.</span></span> <span data-ttu-id="0f6ed-125">La representación de mensajes de excepción o seguimientos de la pila podría:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-125">Rendering exception messages or stack traces could:</span></span>

* <span data-ttu-id="0f6ed-126">Divulgar información confidencial a los usuarios finales.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-126">Disclose sensitive information to end users.</span></span>
* <span data-ttu-id="0f6ed-127">Ayudar a un usuario malintencionado a detectar debilidades en una aplicación que puede poner en peligro la seguridad de la aplicación, el servidor o la red.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-127">Help a malicious user discover weaknesses in an app that can compromise the security of the app, server, or network.</span></span>

## <a name="log-errors-with-a-persistent-provider"></a><span data-ttu-id="0f6ed-128">Registrar errores con un proveedor persistente</span><span class="sxs-lookup"><span data-stu-id="0f6ed-128">Log errors with a persistent provider</span></span>

<span data-ttu-id="0f6ed-129">Si se produce una excepción no controlada, la excepción se registra en <xref:Microsoft.Extensions.Logging.ILogger> las instancias configuradas en el contenedor de servicios.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-129">If an unhandled exception occurs, the exception is logged to <xref:Microsoft.Extensions.Logging.ILogger> instances configured in the service container.</span></span> <span data-ttu-id="0f6ed-130">De forma predeterminada, las aplicaciones increíbles se registran en la salida de la consola con el proveedor de registro de la consola.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-130">By default, Blazor apps log to console output with the Console Logging Provider.</span></span> <span data-ttu-id="0f6ed-131">Considere la posibilidad de iniciar sesión en una ubicación más permanente con un proveedor que administre el tamaño y la rotación del registro.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-131">Consider logging to a more permanent location with a provider that manages log size and log rotation.</span></span> <span data-ttu-id="0f6ed-132">Para obtener más información, consulta <xref:fundamentals/logging/index>.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-132">For more information, see <xref:fundamentals/logging/index>.</span></span>

<span data-ttu-id="0f6ed-133">Durante el desarrollo, el increíble normalmente envía los detalles completos de las excepciones a la consola del explorador para ayudar en la depuración.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-133">During development, Blazor usually sends the full details of exceptions to the browser's console to aid in debugging.</span></span> <span data-ttu-id="0f6ed-134">En producción, los errores detallados en la consola del explorador están deshabilitados de forma predeterminada, lo que significa que los errores no se envían a los clientes, pero los detalles completos de la excepción siguen registrándose en el lado servidor.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-134">In production, detailed errors in the browser's console are disabled by default, which means that errors aren't sent to clients but the exception's full details are still logged server-side.</span></span> <span data-ttu-id="0f6ed-135">Para obtener más información, consulta <xref:fundamentals/error-handling>.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-135">For more information, see <xref:fundamentals/error-handling>.</span></span>

<span data-ttu-id="0f6ed-136">Debe decidir qué incidentes registrar y el nivel de gravedad de los incidentes registrados.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-136">You must decide which incidents to log and the level of severity of logged incidents.</span></span> <span data-ttu-id="0f6ed-137">Es posible que los usuarios hostiles puedan desencadenar errores deliberadamente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-137">Hostile users might be able to trigger errors deliberately.</span></span> <span data-ttu-id="0f6ed-138">Por ejemplo, no registre un incidente de un error en el que `ProductId` se proporcione un desconocido en la dirección URL de un componente que muestre los detalles del producto.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-138">For example, don't log an incident from an error where an unknown `ProductId` is supplied in the URL of a component that displays product details.</span></span> <span data-ttu-id="0f6ed-139">No todos los errores se deben tratar como incidentes de alta gravedad para el registro.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-139">Not all errors should be treated as high-severity incidents for logging.</span></span>

## <a name="places-where-errors-may-occur"></a><span data-ttu-id="0f6ed-140">Lugares donde se pueden producir errores</span><span class="sxs-lookup"><span data-stu-id="0f6ed-140">Places where errors may occur</span></span>

<span data-ttu-id="0f6ed-141">El código de marco de trabajo y la aplicación pueden desencadenar excepciones no controladas en cualquiera de las siguientes ubicaciones:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-141">Framework and app code may trigger unhandled exceptions in any of the following locations:</span></span>

* [<span data-ttu-id="0f6ed-142">Creación de instancias de componentes</span><span class="sxs-lookup"><span data-stu-id="0f6ed-142">Component instantiation</span></span>](#component-instantiation)
* [<span data-ttu-id="0f6ed-143">Métodos de ciclo de vida</span><span class="sxs-lookup"><span data-stu-id="0f6ed-143">Lifecycle methods</span></span>](#lifecycle-methods)
* [<span data-ttu-id="0f6ed-144">Lógica de representación</span><span class="sxs-lookup"><span data-stu-id="0f6ed-144">Rendering logic</span></span>](#rendering-logic)
* [<span data-ttu-id="0f6ed-145">Controladores de eventos</span><span class="sxs-lookup"><span data-stu-id="0f6ed-145">Event handlers</span></span>](#event-handlers)
* [<span data-ttu-id="0f6ed-146">Eliminación de componentes</span><span class="sxs-lookup"><span data-stu-id="0f6ed-146">Component disposal</span></span>](#component-disposal)
* [<span data-ttu-id="0f6ed-147">Interoperabilidad de JavaScript</span><span class="sxs-lookup"><span data-stu-id="0f6ed-147">JavaScript interop</span></span>](#javascript-interop)
* [<span data-ttu-id="0f6ed-148">Controladores de circuito</span><span class="sxs-lookup"><span data-stu-id="0f6ed-148">Circuit handlers</span></span>](#circuit-handlers)
* [<span data-ttu-id="0f6ed-149">Eliminación de circuitos</span><span class="sxs-lookup"><span data-stu-id="0f6ed-149">Circuit disposal</span></span>](#circuit-disposal)
* [<span data-ttu-id="0f6ed-150">Representación previa</span><span class="sxs-lookup"><span data-stu-id="0f6ed-150">Prerendering</span></span>](#prerendering)

<span data-ttu-id="0f6ed-151">Las excepciones no controladas anteriores se describen en las siguientes secciones de este artículo.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-151">The preceding unhandled exceptions are described in the following sections of this article.</span></span>

### <a name="component-instantiation"></a><span data-ttu-id="0f6ed-152">Creación de instancias de componentes</span><span class="sxs-lookup"><span data-stu-id="0f6ed-152">Component instantiation</span></span>

<span data-ttu-id="0f6ed-153">Cuando el increíbles crea una instancia de un componente:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-153">When Blazor creates an instance of a component:</span></span>

* <span data-ttu-id="0f6ed-154">Se invoca el constructor del componente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-154">The component's constructor is invoked.</span></span>
* <span data-ttu-id="0f6ed-155">Se invocan los constructores de cualquier servicio de di no singleton proporcionado al constructor del componente [@inject](xref:blazor/dependency-injection#request-a-service-in-a-component) a través de la Directiva o el atributo [[Insert]](xref:blazor/dependency-injection#request-a-service-in-a-component) .</span><span class="sxs-lookup"><span data-stu-id="0f6ed-155">The constructors of any non-singleton DI services supplied to the component's constructor via the [@inject](xref:blazor/dependency-injection#request-a-service-in-a-component) directive or the [[Inject]](xref:blazor/dependency-injection#request-a-service-in-a-component) attribute are invoked.</span></span> 

<span data-ttu-id="0f6ed-156">Se produce un error en un circuito cuando cualquier constructor ejecutado o `[Inject]` un establecedor para cualquier propiedad produce una excepción no controlada.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-156">A circuit fails when any executed constructor or a setter for any `[Inject]` property throws an unhandled exception.</span></span> <span data-ttu-id="0f6ed-157">La excepción es grave porque el marco no puede crear una instancia del componente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-157">The exception is fatal because the framework can't instantiate the component.</span></span> <span data-ttu-id="0f6ed-158">Si la lógica del constructor puede producir excepciones, la aplicación debe interceptar las excepciones mediante una instrucción [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) con control de errores y registro.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-158">If constructor logic may throw exceptions, the app should trap the exceptions using a [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statement with error handling and logging.</span></span>

### <a name="lifecycle-methods"></a><span data-ttu-id="0f6ed-159">Métodos de ciclo de vida</span><span class="sxs-lookup"><span data-stu-id="0f6ed-159">Lifecycle methods</span></span>

<span data-ttu-id="0f6ed-160">Durante el ciclo de vida de un componente, el increíblemente llama a los métodos de ciclo de vida:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-160">During the lifetime of a component, Blazor invokes lifecycle methods:</span></span>

* `OnInitialized` / `OnInitializedAsync`
* `OnParametersSet` / `OnParametersSetAsync`
* `ShouldRender` / `ShouldRenderAsync`
* `OnAfterRender` / `OnAfterRenderAsync`

<span data-ttu-id="0f6ed-161">Si cualquier método de ciclo de vida produce una excepción, de forma sincrónica o asincrónica, la excepción es grave para el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-161">If any lifecycle method throws an exception, synchronously or asynchronously, the exception is fatal to the circuit.</span></span> <span data-ttu-id="0f6ed-162">Para que los componentes traten los errores de los métodos de ciclo de vida, agregue la lógica de control de errores.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-162">For components to deal with errors in lifecycle methods, add error handling logic.</span></span>

<span data-ttu-id="0f6ed-163">En el ejemplo siguiente, `OnParametersSetAsync` donde llama a un método para obtener un producto:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-163">In the following example where `OnParametersSetAsync` calls a method to obtain a product:</span></span>

* <span data-ttu-id="0f6ed-164">Una excepción iniciada en `ProductRepository.GetProductByIdAsync` el método se controla mediante `try-catch` una instrucción.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-164">An exception thrown in the `ProductRepository.GetProductByIdAsync` method is handled by a `try-catch` statement.</span></span>
* <span data-ttu-id="0f6ed-165">Cuando se `catch` ejecuta el bloque:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-165">When the `catch` block is executed:</span></span>
  * <span data-ttu-id="0f6ed-166">`loadFailed`está establecido en `true`, que se usa para mostrar un mensaje de error al usuario.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-166">`loadFailed` is set to `true`, which is used to display an error message to the user.</span></span>
  * <span data-ttu-id="0f6ed-167">El error se registra.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-167">The error is logged.</span></span>

[!code-cshtml[](handle-errors/samples_snapshot/3.x/product-details.razor?highlight=11,27-39)]

### <a name="rendering-logic"></a><span data-ttu-id="0f6ed-168">Lógica de representación</span><span class="sxs-lookup"><span data-stu-id="0f6ed-168">Rendering logic</span></span>

<span data-ttu-id="0f6ed-169">El marcado declarativo `.razor` de un archivo de componente se C# compila en `BuildRenderTree`un método denominado.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-169">The declarative markup in a `.razor` component file is compiled into a C# method called `BuildRenderTree`.</span></span> <span data-ttu-id="0f6ed-170">Cuando un componente se representa, `BuildRenderTree` ejecuta y genera una estructura de datos que describe los elementos, el texto y los componentes secundarios del componente representado.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-170">When a component renders, `BuildRenderTree` executes and builds up a data structure describing the elements, text, and child components of the rendered component.</span></span>

<span data-ttu-id="0f6ed-171">La lógica de representación puede producir una excepción.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-171">Rendering logic can throw an exception.</span></span> <span data-ttu-id="0f6ed-172">Un ejemplo de este escenario se produce `@someObject.PropertyName` cuando se evalúa `@someObject` pero `null`es.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-172">An example of this scenario occurs when `@someObject.PropertyName` is evaluated but `@someObject` is `null`.</span></span> <span data-ttu-id="0f6ed-173">Una excepción no controlada producida por la lógica de representación es grave para el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-173">An unhandled exception thrown by rendering logic is fatal to the circuit.</span></span>

<span data-ttu-id="0f6ed-174">Para evitar una excepción de referencia nula en la lógica de representación, busque `null` un objeto antes de tener acceso a sus miembros.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-174">To prevent a null reference exception in rendering logic, check for a `null` object before accessing its members.</span></span> <span data-ttu-id="0f6ed-175">En el ejemplo siguiente, `person.Address` no se tiene acceso a `person.Address` las `null`propiedades si es:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-175">In the following example, `person.Address` properties aren't accessed if `person.Address` is `null`:</span></span>

[!code-cshtml[](handle-errors/samples_snapshot/3.x/person-example.razor?highlight=1)]

<span data-ttu-id="0f6ed-176">En el código anterior se `person` supone `null`que no es.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-176">The preceding code assumes that `person` isn't `null`.</span></span> <span data-ttu-id="0f6ed-177">A menudo, la estructura del código garantiza que un objeto existe en el momento en que se representa el componente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-177">Often, the structure of the code guarantees that an object exists at the time the component is rendered.</span></span> <span data-ttu-id="0f6ed-178">En esos casos, no es necesario comprobar `null` la lógica de representación.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-178">In those cases, it isn't necessary to check for `null` in rendering logic.</span></span> <span data-ttu-id="0f6ed-179">En el ejemplo anterior, `person` podría estar garantizado que existe porque `person` se crea cuando se crea una instancia del componente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-179">In the prior example, `person` might be guaranteed to exist because `person` is created when the component is instantiated.</span></span>

### <a name="event-handlers"></a><span data-ttu-id="0f6ed-180">Controladores de eventos</span><span class="sxs-lookup"><span data-stu-id="0f6ed-180">Event handlers</span></span>

<span data-ttu-id="0f6ed-181">El código del lado cliente desencadena invocaciones de código C# cuando se crean controladores de eventos mediante:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-181">Client-side code triggers invocations of C# code when event handlers are created using:</span></span>

* `@onclick`
* `@onchange`
* <span data-ttu-id="0f6ed-182">Otros `@on...` atributos</span><span class="sxs-lookup"><span data-stu-id="0f6ed-182">Other `@on...` attributes</span></span>
* `@bind`

<span data-ttu-id="0f6ed-183">El código del controlador de eventos podría producir una excepción no controlada en estos escenarios.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-183">Event handler code might throw an unhandled exception in these scenarios.</span></span>

<span data-ttu-id="0f6ed-184">Si un controlador de eventos produce una excepción no controlada (por ejemplo, se produce un error en una consulta de base de datos), la excepción es grave para el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-184">If an event handler throws an unhandled exception (for example, a database query fails), the exception is fatal to the circuit.</span></span> <span data-ttu-id="0f6ed-185">Si la aplicación llama a código que podría dar error por motivos externos, Capture las excepciones mediante una instrucción [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) con control de errores y registro.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-185">If the app calls code that could fail for external reasons, trap exceptions using a [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statement with error handling and logging.</span></span>

<span data-ttu-id="0f6ed-186">Si el código de usuario no intercepta y controla la excepción, el marco de trabajo registra la excepción y finaliza el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-186">If user code doesn't trap and handle the exception, the framework logs the exception and terminates the circuit.</span></span>

### <a name="component-disposal"></a><span data-ttu-id="0f6ed-187">Eliminación de componentes</span><span class="sxs-lookup"><span data-stu-id="0f6ed-187">Component disposal</span></span>

<span data-ttu-id="0f6ed-188">Un componente se puede quitar de la interfaz de usuario, por ejemplo, porque el usuario ha navegado a otra página.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-188">A component may be removed from the UI, for example, because the user has navigated to another page.</span></span> <span data-ttu-id="0f6ed-189">Cuando un componente que implementa <xref:System.IDisposable?displayProperty=fullName> se quita de la interfaz de usuario, el marco de trabajo llama al método del <xref:System.IDisposable.Dispose*> componente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-189">When a component that implements <xref:System.IDisposable?displayProperty=fullName> is removed from the UI, the framework calls the component's <xref:System.IDisposable.Dispose*> method.</span></span> 

<span data-ttu-id="0f6ed-190">Si el método del `Dispose` componente produce una excepción no controlada, la excepción es grave para el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-190">If the component's `Dispose` method throws an unhandled exception, the exception is fatal to the circuit.</span></span> <span data-ttu-id="0f6ed-191">Si la lógica de eliminación puede producir excepciones, la aplicación debe interceptar las excepciones mediante una instrucción [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) con control de errores y registro.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-191">If disposal logic may throw exceptions, the app should trap the exceptions using a [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statement with error handling and logging.</span></span>

<span data-ttu-id="0f6ed-192">Para obtener más información sobre la eliminación de <xref:blazor/components#component-disposal-with-idisposable>componentes, vea.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-192">For more information on component disposal, see <xref:blazor/components#component-disposal-with-idisposable>.</span></span>

### <a name="javascript-interop"></a><span data-ttu-id="0f6ed-193">Interoperabilidad de JavaScript</span><span class="sxs-lookup"><span data-stu-id="0f6ed-193">JavaScript interop</span></span>

<span data-ttu-id="0f6ed-194">`IJSRuntime.InvokeAsync<T>`permite que el código .NET realice llamadas asincrónicas al tiempo de ejecución de JavaScript en el explorador del usuario.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-194">`IJSRuntime.InvokeAsync<T>` allows .NET code to make asynchronous calls to the JavaScript runtime in the user's browser.</span></span>

<span data-ttu-id="0f6ed-195">Las condiciones siguientes se aplican al control `InvokeAsync<T>`de errores con:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-195">The following conditions apply to error handling with `InvokeAsync<T>`:</span></span>

* <span data-ttu-id="0f6ed-196">Si una llamada a `InvokeAsync<T>` produce un error sincrónicamente, se produce una excepción de .net.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-196">If a call to `InvokeAsync<T>` fails synchronously, a .NET exception occurs.</span></span> <span data-ttu-id="0f6ed-197">Por ejemplo, `InvokeAsync<T>` se produce una llamada a My, porque los argumentos proporcionados no se pueden serializar.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-197">A call to `InvokeAsync<T>` my fail, for example, because the supplied arguments can't be serialized.</span></span> <span data-ttu-id="0f6ed-198">El código del desarrollador debe detectar la excepción.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-198">Developer code must catch the exception.</span></span> <span data-ttu-id="0f6ed-199">Si el código de la aplicación en un controlador de eventos o un método de ciclo de vida de componente no controla una excepción, la excepción resultante es grave para el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-199">If app code in an event handler or component lifecycle method doesn't handle an exception, the resulting exception is fatal to the circuit.</span></span>
* <span data-ttu-id="0f6ed-200">Si se produce un `InvokeAsync<T>` error de forma asincrónica en <xref:System.Threading.Tasks.Task> una llamada a, se produce un error de .net.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-200">If a call to `InvokeAsync<T>` fails asynchronously, the .NET <xref:System.Threading.Tasks.Task> fails.</span></span> <span data-ttu-id="0f6ed-201">Una llamada a `InvokeAsync<T>` puede producir un error, por ejemplo, porque el código de JavaScript produce una excepción o devuelve un `Promise` que se completó como `rejected`.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-201">A call to `InvokeAsync<T>` may fail, for example, because the JavaScript-side code throws an exception or returns a `Promise` that completed as `rejected`.</span></span> <span data-ttu-id="0f6ed-202">El código del desarrollador debe detectar la excepción.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-202">Developer code must catch the exception.</span></span> <span data-ttu-id="0f6ed-203">Si usa el operador [Await](/dotnet/csharp/language-reference/keywords/await) , considere la posibilidad de encapsular la llamada al método en una instrucción [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) con control de errores y registro.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-203">If using the [await](/dotnet/csharp/language-reference/keywords/await) operator, consider wrapping the method call in a [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statement with error handling and logging.</span></span> <span data-ttu-id="0f6ed-204">De lo contrario, el código que genera el error provoca una excepción no controlada que es grave para el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-204">Otherwise, the failing code results in an unhandled exception that's fatal to the circuit.</span></span>
* <span data-ttu-id="0f6ed-205">De forma predeterminada, las `InvokeAsync<T>` llamadas a deben completarse en un período determinado o, de lo contrario, se agota el tiempo de espera de la llamada. El período de tiempo de espera predeterminado es de un minuto.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-205">By default, calls to `InvokeAsync<T>` must complete within a certain period or else the call times out. The default timeout period is one minute.</span></span> <span data-ttu-id="0f6ed-206">El tiempo de espera protege el código contra una pérdida en la conectividad de red o código JavaScript que nunca devuelve un mensaje de finalización.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-206">The timeout protects the code against a loss in network connectivity or JavaScript code that never sends back a completion message.</span></span> <span data-ttu-id="0f6ed-207">Si `Task` se agota el tiempo de espera de la llamada, <xref:System.OperationCanceledException>se produce un error con una.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-207">If the call times out, the resulting `Task` fails with an <xref:System.OperationCanceledException>.</span></span> <span data-ttu-id="0f6ed-208">Capture y procese la excepción con el registro.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-208">Trap and process the exception with logging.</span></span>

<span data-ttu-id="0f6ed-209">Del mismo modo, el código de JavaScript puede iniciar llamadas a métodos .NET indicados por el [atributo [JSInvokable]](xref:blazor/javascript-interop#invoke-net-methods-from-javascript-functions).</span><span class="sxs-lookup"><span data-stu-id="0f6ed-209">Similarly, JavaScript code may initiate calls to .NET methods indicated by the [[JSInvokable] attribute](xref:blazor/javascript-interop#invoke-net-methods-from-javascript-functions).</span></span> <span data-ttu-id="0f6ed-210">Si estos métodos .NET producen una excepción no controlada:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-210">If these .NET methods throw an unhandled exception:</span></span>

* <span data-ttu-id="0f6ed-211">La excepción no se trata como grave para el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-211">The exception isn't treated as fatal to the circuit.</span></span>
* <span data-ttu-id="0f6ed-212">Se rechaza el lado `Promise` de JavaScript.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-212">The JavaScript-side `Promise` is rejected.</span></span>

<span data-ttu-id="0f6ed-213">Tiene la opción de usar el código de control de errores en el lado de .NET o en el lado de JavaScript de la llamada al método.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-213">You have the option of using error handling code on either the .NET side or the JavaScript side of the method call.</span></span>

<span data-ttu-id="0f6ed-214">Para obtener más información, consulta <xref:blazor/javascript-interop>.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-214">For more information, see <xref:blazor/javascript-interop>.</span></span>

### <a name="circuit-handlers"></a><span data-ttu-id="0f6ed-215">Controladores de circuito</span><span class="sxs-lookup"><span data-stu-id="0f6ed-215">Circuit handlers</span></span>

<span data-ttu-id="0f6ed-216">Increíbles permite que el código defina un *controlador de circuito*, que recibe notificaciones cuando cambia el estado del circuito de un usuario.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-216">Blazor allows code to define a *circuit handler*, which receives notifications when the state of a user's circuit changes.</span></span> <span data-ttu-id="0f6ed-217">Se utilizan los siguientes Estados:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-217">The following states are used:</span></span>

* `initialized`
* `connected`
* `disconnected`
* `disposed`

<span data-ttu-id="0f6ed-218">Las notificaciones se administran registrando un servicio di que hereda de la `CircuitHandler` clase base abstracta.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-218">Notifications are managed by registering a DI service that inherits from the `CircuitHandler` abstract base class.</span></span>

<span data-ttu-id="0f6ed-219">Si los métodos de un controlador de circuito personalizado producen una excepción no controlada, la excepción es grave para el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-219">If a custom circuit handler's methods throw an unhandled exception, the exception is fatal to the circuit.</span></span> <span data-ttu-id="0f6ed-220">Para tolerar excepciones en el código de un controlador o en métodos denominados, ajuste el código en una o varias instrucciones [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) con control de errores y registro.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-220">To tolerate exceptions in a handler's code or called methods, wrap the code in one or more [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statements with error handling and logging.</span></span>

### <a name="circuit-disposal"></a><span data-ttu-id="0f6ed-221">Eliminación de circuitos</span><span class="sxs-lookup"><span data-stu-id="0f6ed-221">Circuit disposal</span></span>

<span data-ttu-id="0f6ed-222">Cuando un circuito finaliza porque un usuario se ha desconectado y el marco está limpiando el estado del circuito, el marco de trabajo desecha el ámbito de DI del circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-222">When a circuit ends because a user has disconnected and the framework is cleaning up the circuit state, the framework disposes of the circuit's DI scope.</span></span> <span data-ttu-id="0f6ed-223">Al desechar el ámbito, se elimina cualquier servicio DI con ámbito de circuito que <xref:System.IDisposable?displayProperty=fullName>implemente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-223">Disposing the scope disposes any circuit-scoped DI services that implement <xref:System.IDisposable?displayProperty=fullName>.</span></span> <span data-ttu-id="0f6ed-224">Si un servicio DI produce una excepción no controlada durante la eliminación, el marco de trabajo registra la excepción.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-224">If any DI service throws an unhandled exception during disposal, the framework logs the exception.</span></span>

### <a name="prerendering"></a><span data-ttu-id="0f6ed-225">Representación previa</span><span class="sxs-lookup"><span data-stu-id="0f6ed-225">Prerendering</span></span>

<span data-ttu-id="0f6ed-226">Los componentes increíbles se pueden representar con `Html.RenderComponentAsync` el fin de que su marca HTML representada se devuelva como parte de la solicitud HTTP inicial del usuario.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-226">Blazor components can be prerendered using `Html.RenderComponentAsync` so that their rendered HTML markup is returned as part of the user's initial HTTP request.</span></span> <span data-ttu-id="0f6ed-227">Esto funciona de la siguiente manera:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-227">This works by:</span></span>

* <span data-ttu-id="0f6ed-228">Crear un nuevo circuito que contenga todos los componentes prerepresentados que forman parte de la misma página.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-228">Creating a new circuit containing all of the prerendered components that are part of the same page.</span></span>
* <span data-ttu-id="0f6ed-229">Generar el código HTML inicial.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-229">Generating the initial HTML.</span></span>
* <span data-ttu-id="0f6ed-230">Tratamiento del circuito como `disconnected` hasta que el explorador del usuario establece una conexión signalr en el mismo servidor para reanudar la interactividad en el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-230">Treating the circuit as `disconnected` until the user's browser establishes a SignalR connection back to the same server to resume interactivity on the circuit.</span></span>

<span data-ttu-id="0f6ed-231">Si algún componente produce una excepción no controlada durante la representación previa, por ejemplo, durante un método de ciclo de vida o en la lógica de representación:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-231">If any component throws an unhandled exception during prerendering, for example, during a lifecycle method or in rendering logic:</span></span>

* <span data-ttu-id="0f6ed-232">La excepción es grave para el circuito.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-232">The exception is fatal to the circuit.</span></span>
* <span data-ttu-id="0f6ed-233">La excepción se inicia en la pila de llamadas de `Html.RenderComponentAsync` la llamada.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-233">The exception is thrown up the call stack from the `Html.RenderComponentAsync` call.</span></span> <span data-ttu-id="0f6ed-234">Por lo tanto, se produce un error en toda la solicitud HTTP a menos que el código del desarrollador detecte la excepción explícitamente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-234">Therefore, the entire HTTP request fails unless the exception is explicitly caught by developer code.</span></span>

<span data-ttu-id="0f6ed-235">En circunstancias normales, cuando se produce un error en la representación previa, la generación y representación del componente no tiene sentido, ya que no se puede representar un componente de trabajo.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-235">Under normal circumstances when prerendering fails, continuing to build and render the component doesn't make sense because a working component can't be rendered.</span></span>

<span data-ttu-id="0f6ed-236">Para tolerar los errores que pueden producirse durante la representación previa, la lógica de control de errores debe colocarse dentro de un componente que pueda producir excepciones.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-236">To tolerate errors that may occur during prerendering, error handling logic must be placed inside a component that may throw exceptions.</span></span> <span data-ttu-id="0f6ed-237">Use instrucciones [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) con control de errores y registro.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-237">Use [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statements with error handling and logging.</span></span> <span data-ttu-id="0f6ed-238">En lugar de encapsular la llamada `RenderComponentAsync` a en `try-catch` una instrucción, coloque la lógica de control de errores en el `RenderComponentAsync`componente representado por.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-238">Instead of wrapping the call to `RenderComponentAsync` in a `try-catch` statement, place error handling logic in the component rendered by `RenderComponentAsync`.</span></span>

## <a name="advanced-scenarios"></a><span data-ttu-id="0f6ed-239">Escenarios avanzados</span><span class="sxs-lookup"><span data-stu-id="0f6ed-239">Advanced scenarios</span></span>

### <a name="recursive-rendering"></a><span data-ttu-id="0f6ed-240">Representación recursiva</span><span class="sxs-lookup"><span data-stu-id="0f6ed-240">Recursive rendering</span></span>

<span data-ttu-id="0f6ed-241">Los componentes se pueden anidar de forma recursiva.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-241">Components can be nested recursively.</span></span> <span data-ttu-id="0f6ed-242">Esto resulta útil para representar estructuras de datos recursivas.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-242">This is useful for representing recursive data structures.</span></span> <span data-ttu-id="0f6ed-243">Por ejemplo, un `TreeNode` componente puede representar más `TreeNode` componentes para cada uno de los elementos secundarios del nodo.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-243">For example, a `TreeNode` component can render more `TreeNode` components for each of the node's children.</span></span>

<span data-ttu-id="0f6ed-244">Cuando se represente de forma recursiva, evite patrones de codificación que produzcan una recursividad infinita:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-244">When rendering recursively, avoid coding patterns that result in infinite recursion:</span></span>

* <span data-ttu-id="0f6ed-245">No represente de forma recursiva una estructura de datos que contenga un ciclo.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-245">Don't recursively render a data structure that contains a cycle.</span></span> <span data-ttu-id="0f6ed-246">Por ejemplo, no represente un nodo de árbol cuyos elementos secundarios se incluyan a sí mismos.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-246">For example, don't render a tree node whose children includes itself.</span></span>
* <span data-ttu-id="0f6ed-247">No cree una cadena de diseños que contengan un ciclo.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-247">Don't create a chain of layouts that contain a cycle.</span></span> <span data-ttu-id="0f6ed-248">Por ejemplo, no cree un diseño cuyo diseño sea el mismo.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-248">For example, don't create a layout whose layout is itself.</span></span>
* <span data-ttu-id="0f6ed-249">No permita que un usuario final viole invariantes de recursividad (reglas) a través de entradas de datos malintencionadas o llamadas de interoperabilidad de JavaScript.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-249">Don't allow an end user to violate recursion invariants (rules) through malicious data entry or JavaScript interop calls.</span></span>

<span data-ttu-id="0f6ed-250">Bucles infinitos durante la representación:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-250">Infinite loops during rendering:</span></span>

* <span data-ttu-id="0f6ed-251">Hace que el proceso de representación continúe indefinidamente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-251">Causes the rendering process to continue forever.</span></span>
* <span data-ttu-id="0f6ed-252">Es equivalente a crear un bucle sin terminar.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-252">Is equivalent to creating an unterminated loop.</span></span>

<span data-ttu-id="0f6ed-253">En estos casos, el circuito afectado se bloquea y el subproceso normalmente intenta:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-253">In these scenarios, the affected circuit hangs, and the thread usually attempts to:</span></span>

* <span data-ttu-id="0f6ed-254">Consume el tiempo de CPU permitido por el sistema operativo, indefinidamente.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-254">Consume as much CPU time as permitted by the operating system, indefinitely.</span></span>
* <span data-ttu-id="0f6ed-255">Consume una cantidad ilimitada de memoria del servidor.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-255">Consume an unlimited amount of server memory.</span></span> <span data-ttu-id="0f6ed-256">Consumir memoria ilimitada es equivalente al escenario en el que un bucle sin terminar agrega entradas a una colección en cada iteración.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-256">Consuming unlimited memory is equivalent to the scenario where an unterminated loop adds entries to a collection on every iteration.</span></span>

<span data-ttu-id="0f6ed-257">Para evitar patrones infinitos de recursividad, asegúrese de que el código de representación recursivo contiene condiciones de detención adecuadas.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-257">To avoid infinite recursion patterns, ensure that recursive rendering code contains suitable stopping conditions.</span></span>

### <a name="custom-render-tree-logic"></a><span data-ttu-id="0f6ed-258">Lógica de árbol de representación personalizada</span><span class="sxs-lookup"><span data-stu-id="0f6ed-258">Custom render tree logic</span></span>

<span data-ttu-id="0f6ed-259">La mayoría de los componentes más increíbles se implementan como archivos *. Razor* y se compilan para `RenderTreeBuilder` generar lógica que funcione en un para representar la salida.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-259">Most Blazor components are implemented as *.razor* files and are compiled to produce logic that operates on a `RenderTreeBuilder` to render their output.</span></span> <span data-ttu-id="0f6ed-260">Un desarrollador puede implementar `RenderTreeBuilder` manualmente la lógica mediante código de procedimiento. C#</span><span class="sxs-lookup"><span data-stu-id="0f6ed-260">A developer may manually implement `RenderTreeBuilder` logic using procedural C# code.</span></span> <span data-ttu-id="0f6ed-261">Para obtener más información, consulta <xref:blazor/components#manual-rendertreebuilder-logic>.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-261">For more information, see <xref:blazor/components#manual-rendertreebuilder-logic>.</span></span>

> [!WARNING]
> <span data-ttu-id="0f6ed-262">El uso de la lógica del generador de árboles de representación manual se considera un escenario avanzado y no seguro, no recomendado para el desarrollo de componentes generales.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-262">Use of manual render tree builder logic is considered an advanced and unsafe scenario, not recommended for general component development.</span></span>

<span data-ttu-id="0f6ed-263">Si `RenderTreeBuilder` se escribe código, el desarrollador debe garantizar la corrección del código.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-263">If `RenderTreeBuilder` code is written, the developer must guarantee the correctness of the code.</span></span> <span data-ttu-id="0f6ed-264">Por ejemplo, el desarrollador debe asegurarse de que:</span><span class="sxs-lookup"><span data-stu-id="0f6ed-264">For example, the developer must ensure that:</span></span>

* <span data-ttu-id="0f6ed-265">Las llamadas `OpenElement` a `CloseElement` y están correctamente equilibradas.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-265">Calls to `OpenElement` and `CloseElement` are correctly balanced.</span></span>
* <span data-ttu-id="0f6ed-266">Los atributos solo se agregan en los lugares correctos.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-266">Attributes are only added in the correct places.</span></span>

<span data-ttu-id="0f6ed-267">Una lógica incorrecta del generador de árboles de representación manual puede producir un comportamiento indefinido arbitrario, incluidos los bloqueos, los bloqueos del servidor y las vulnerabilidades de seguridad.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-267">Incorrect manual render tree builder logic can cause arbitrary undefined behavior, including crashes, server hangs, and security vulnerabilities.</span></span>

<span data-ttu-id="0f6ed-268">Considere la posibilidad de representar la lógica del generador de árbol de representación manual en el mismo nivel de complejidad y con el mismo nivel de *riesgo* que escribir manualmente código de ensamblado o instrucciones MSIL.</span><span class="sxs-lookup"><span data-stu-id="0f6ed-268">Consider manual render tree builder logic on the same level of complexity and with the same level of *danger* as writing assembly code or MSIL instructions by hand.</span></span>
